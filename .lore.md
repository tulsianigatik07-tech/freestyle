<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019f227c-cea0-7301-a761-33870d8659b3 -->
* **AGENTS.md and CLAUDE.md: agent context entry points at repo root**: \`AGENTS.md\` (330 bytes originally, expanded) is the primary agent context file — contains a lore-managed block pointing to \`.lore.md\` plus an \`## Engineering specs & audits (specs/)\` section listing spec files and distinguishing them from \`apps/docs/\`. \`CLAUDE.md\` at repo root is a real pointer/reference file (not a symlink) to \`AGENTS.md\`, so Claude Code also discovers \`specs/\` context. Lore auto-updates flow through \`.lore.md\` which \`AGENTS.md\` references. Both files serve as entry points for different agent runtimes (OpenCode reads \`AGENTS.md\`/\`.lore.md\`; Claude Code reads \`CLAUDE.md\`).

<!-- lore:019f2224-4338-75f7-922f-0fe7facb23a8 -->
* **apps/docs: Mintlify docs migrated into monorepo at apps/docs/**: Documentation migrated from standalone \`freestyle-voice/docs\` repo into \`apps/docs/\` within the monorepo. Uses Mintlify theme with \`docs.json\` config. 22 files total: 6 migrated MDX pages, 6 new MDX pages, assets in \`images/\` and \`logo/\`. Navigation: 3 groups (Getting Started, Plugins, Advanced). \`apps/docs\` is a pnpm workspace member (covered by \`apps/\*\` glob). \`apps/docs/package.json\` has \`"dev": "mintlify dev"\` script and \`mintlify: latest\` devDependency. Turbo's \`dev\` task picks up \`apps/docs\` automatically — no manual root script needed. \`templates/\` pattern does NOT apply here — docs IS a workspace member unlike templates.

<!-- lore:019f227e-a37b-76e3-9b7d-f2696e5b1de3 -->
* **apps/docs: Mintlify docs site — excluded from default turbo tasks, knip, and CI build checks**: \`apps/docs/\` is a Mintlify docs site in the monorepo. \`knip.jsonc\` explicitly excludes it: 'Docs site is a standalone Mintlify project, not a pnpm workspace member — never imported or built within the monorepo.' \`pnpm dev\` and \`pnpm build\` use \`--filter=!docs\`. Separate \`dev:docs\` script: \`turbo dev --filter=docs\`. \`.github/workflows/build.yml\` bypass pattern includes \`apps/docs/\*\`. Logo: SVG files at \`apps/docs/logo/freestyle-light.svg\` and \`apps/docs/logo/freestyle-dark.svg\` (Instrument Serif italic + waveform polyline, self-contained with Google Fonts). Navbar: Discord link uses \`type: 'discord'\` (shows online count), GitHub link uses \`type: 'github'\` (shows star count). No Download button in navbar. GitHub anchor removed from sidebar; Blog anchor kept.

<!-- lore:019ef818-39a8-7346-bc15-eb09a03bbc2e -->
* **apps/electron env file loading: only .env.local is loaded, .env.example is onboarding template**: \`apps/electron/src/main/index.ts:31-38\` dev-only block calls \`proc.loadEnvFile?.('.env.local')\` — loads ONLY \`.env.local\`, never \`.env\` or \`.env.example\`. \`.gitignore\` rules (lines 60-64): \`.env\`, \`.env.local\`, \`.env.\*.local\` are ignored; \`!.env.example\` is explicitly whitelisted. \`.env.example\` is tracked in git, documented in \`CONTRIBUTING.md:105/115\` as the onboarding template (copy → \`.env.local\`). Do NOT remove \`.env.example\`. Any stray \`apps/electron/.env\` file is gitignored and never loaded — likely created by mistake instead of \`.env.local\`.

<!-- lore:019f2224-433f-78f6-bfca-64ea017a7d75 -->
* **apps/server HTTP API: 55 endpoints across 17 route files, 15 MCP tools**: Full API surface: 55 HTTP endpoints + WebSocket upgrade + MCP catch-all, 15 MCP tools, 17 route files in \`apps/server/src/routes/\`. Key domains: \`/api/settings\`, \`/api/keys\`, \`/api/auth\`, \`/api/models\`, \`/api/history\`, \`/api/transcribe\`, \`/api/formats\`, \`/api/dictionary\`, \`/api/vocabulary\`, \`/api/plugins\`, \`/api/health\` (always auth-exempt). \`GET /\` returns \`"Freestyle API"\`. \`POST /api/telemetry\` relays renderer events. \`POST /api/client-error\` logs renderer crashes. MCP mounted at \`/mcp\`, stream at \`/stream\`. Full inventory documented in \`apps/docs/api-reference.mdx\`.

<!-- lore:019ef818-39a6-7919-b8ee-1d465f3e7bb6 -->
* **apps/server uses process.env (not c.env) — it is a Node/Hono app, not a Cloudflare Worker**: \`apps/server\` is a Node-based Hono app embedded in Electron (port 4649). All config is via \`process.env\`. \`c.env\` (Hono's Cloudflare Workers bindings pattern) appears zero times — it belongs to the separate \`cloud\` repo Worker only. Trap: Hono is used in both places, so \`c.env\` looks applicable. Fix: never use \`c.env\` in \`apps/server\`; always use \`process.env\`. Entry point: \`apps/server/src/index.ts\`; default export is \`createApp\` factory (NOT an app instance). \`startServer()\` initializes plugins then calls \`createApp(pluginMiddleware)\`. Env vars: PORT, HOST, FREESTYLE\_AUTH\_TOKEN, FREESTYLE\_DB\_PATH, FREESTYLE\_ENV, FREESTYLE\_ANALYTICS\_DEV, DO\_NOT\_TRACK, FREESTYLE\_MLX\_ASR\_\*, HF\_HOME, FREESTYLE\_CLOUD\_URL.

<!-- lore:019f0340-6c2b-7525-b97c-4b33dbd3a655 -->
* **Built-in server plugins: auth and cloud-sync in apps/server/src/lib/plugins/builtin/**: Auth and cloud-sync are built-in server plugins in \`apps/server/src/lib/plugins/builtin/\`. Built-ins are passed via \`builtin\` option to \`loadPlugins()\` — they bypass the npm import/factory flow but \`setup()\` is still called. Built-ins are prepended before user plugins so auth (enforce: pre) always runs first. Built-ins are not disable-able via settings. \`initServerPlugins({ token })\` wires in both built-ins. \`syncCleanupPreferences\` was moved out of \`settings.ts\` into the cloud-sync plugin. \`setAuthToken\` is now only used in \`auth.ts\` (builtin) and \`tests/auth.test.ts\`.

<!-- lore:019eda8f-2bf4-78ec-b979-8852b205f3dc -->
* **Cleanup intensity/prompt resolution: resolveBaseCleanupPrompt + preset fallback**: \`prompts.ts\` uses \`resolveBaseCleanupPrompt(intensity: CleanupIntensity, customPrompt?: string): string\` to select the active system prompt. For \`"custom"\`: uses trimmed user prompt if non-empty, else falls back to \`CLEANUP\_PRESET\_PROMPTS.low\`. For other intensities: returns \`CLEANUP\_PRESET\_PROMPTS\[intensity]\` directly. \`buildRewritePrompt\` accepts \`intensity\` and \`customPrompt\` options and delegates to this resolver (default intensity: \`"low"\`). Preset bodies live in \`@freestyle/validations\` (\`cleanup-presets.ts\`), not inline in \`prompts.ts\` — the old \`UNIFIED\_REWRITE\_SYSTEM\` constant was removed. \`DEFAULT\_CLEANUP\_INTENSITY = "low"\` and \`CLEANUP\_CUSTOM\_PROMPT\_MAX = 20000\` are defined in \`validations/settings.ts\`.

<!-- lore:019efae2-439a-75b0-b41c-7066fb1885bd -->
* **Craft independent release pipelines: per-package .craft.yml with tagPrefix**: Monorepo uses separate \`.craft.yml\` per releasable unit for independent versioning. App: \`/.craft.yml\` (no tagPrefix, no npm). SDK: \`packages/sdk/.craft.yml\` (tagPrefix \`freestyle-voice@\`, releaseBranchPrefix \`release-sdk\`). Audio plugin: \`plugins/audio-transcription/.craft.yml\` (tagPrefix \`plugin-audio-transcription@\`, releaseBranchPrefix \`release-plugin-audio-transcription\`). Craft's \`findConfigFile()\` walks CWD upward — running from a package subdirectory finds that package's config first. \`tagPrefix\` on the \`github\` target prevents git tag collisions across packages. All package pipelines use \`versioning: { policy: manual }\` (explicit semver per release). OIDC trusted publishing requires one-time manual npm publish to bootstrap each package before OIDC can be configured.

<!-- lore:019efae2-439d-7634-86be-442ea81c46d0 -->
* **Craft release workflows: release-package.yml + publish-package.yml for npm packages**: \`release-package.yml\`: \`workflow\_dispatch\` with \`package\` (choice: sdk/plugin-audio-transcription) and \`version\` (explicit semver) inputs. Runs \`getsentry/craft@v2\` with \`path:\` set to the package subdirectory. Labels publish-request issue \`pkg:\<name>\` for routing. \`publish-package.yml\`: triggers on \`accepted\` label; routes by \`pkg:\*\` label to correct package directory; waits for Package Artifacts workflow; runs \`craft publish\` with npm OIDC trusted publishing (\`id-token: write\` permission). Adding a new plugin requires updating the \`package\` choices in \`release-package.yml\`. Existing \`release.yml\`/\`publish.yml\` handle the Electron app and are unchanged.

<!-- lore:019f240c-997b-7562-b034-67a79eb6e33b -->
* **deliverOutput() empty-text suppression: mode=None or empty text skips paste automatically**: \`deliverOutput()\` in \`apps/electron/src/main/index.ts:999\`: if \`out.mode === OutputMode.None || !out.text?.trim()\` → emits \`OutputDelivered\` with \`mode: OutputMode.None\` and returns immediately — no paste occurs. This means a plugin returning empty string from \`afterTranscribe\` (or a future suppress signal) automatically prevents output delivery with no cross-process coordination needed. \`beforeOutput\` hook runs first; paste errors call \`notifyPasteFailed()\` and emit \`PipelineError\` with \`stage: PipelineStage.Output\`.

<!-- lore:019ee7ed-0eb7-76e6-8a8e-feb96e665987 -->
* **Electron diagnostics: crash handlers, renderer error forwarding, IPC channels**: Main-process crash handlers use \`process.on('uncaughtExceptionMonitor')\` + \`process.on('unhandledRejection')\` (not \`uncaughtException\` — monitor variant avoids changing Node crash behavior). Renderer errors captured via \`window.addEventListener('error'/'unhandledrejection')\` and forwarded through preload IPC (\`logs:renderer-error\`) so main logs them into the same session file. IPC channels: \`logs:renderer-error\`, \`logs:reveal\`, \`logs:export\`, \`logs:get-path\`. Preload exposes \`logRendererError\`, \`revealLogs\`, \`exportLogs\`. Settings → Data panel gets a Diagnostics row with Reveal/Export buttons. \`captureException\` lives in \`apps/server/src/lib/posthog.ts\` and is NOT exported from the package — Electron main cannot import it directly; PostHog reporting from main must go through local HTTP once server is available.

<!-- lore:019ef818-39a4-7ce3-9f00-cadd4ef43fff -->
* **FREESTYLE\_CLOUD\_URL env var: server-side override for cloud Worker URL**: \`FREESTYLE\_CLOUD\_URL\` is consumed exclusively in \`apps/server/src/lib/freestyle-cloud.ts\`. \`freestyleCloudUrl()\` (line 73-78) returns \`(process.env.FREESTYLE\_CLOUD\_URL || DEFAULT\_CLOUD\_URL).replace(/\\/+$/, '')\` where \`DEFAULT\_CLOUD\_URL = 'https://service.freestylevoice.com'\`. Used in 5 call sites: Better-auth device-flow base, \`fetchCloudUser\` (\`/v1/me\`), \`signOutCloud\`, \`cloudJson\` helper, and auth route host. In dev, set \`FREESTYLE\_CLOUD\_URL=http://localhost:8787\` in \`apps/electron/.env.local\` (port 8787 = \`wrangler dev\` for the separate \`cloud\` repo, NOT the embedded server at 4649). In production builds, \`.env.local\` is not loaded; default URL is used automatically.

<!-- lore:019f1407-8c67-7621-bf04-568e694d4555 -->
* **Language pipeline: auto-detect flows as undefined through ASR and LLM cleanup stages**: \`normalizeLanguageSetting()\` returns \`undefined\` for \`null\`, \`undefined\`, or \`'auto'\`. \`getLanguageSetting()\` reads the \`settings\` table and returns \`undefined\` for auto. In \`transcribe.ts\` line 222, language is omitted from the ASR call when \`undefined\`. In \`post-process.ts\`, \`undefined\` is passed directly to \`buildRewritePrompt()\` → \`buildLanguageBlock()\`. Whisper-local sends \`opts.language ?? 'auto'\` as the wire value. ElevenLabs uses key \`languageCode\`; all other providers use \`language\`. \`aiSdkProviderOptions()\` skips the language key entirely when falsy or \`'auto'\`. The LLM cleanup stage (not ASR) is where translation bugs manifest when the language constraint is absent.

<!-- lore:019f0ebe-4535-7651-a2fc-7aa5c6c169f9 -->
* **loadServerPlugins() in apps/server: reads settings, filters disabled, calls SDK loadPlugins()**: \`apps/server/src/lib/plugins/loader.ts\` (54 lines): \`loadServerPlugins(builtin: Plugin\[] = \[])\` reads \`disabled\_plugins\` via \`parseDisabledPlugins(readSetting('disabled\_plugins'))\`, reads \`plugins\` via \`parsePluginsSetting(readSetting('plugins'))\`, filters out disabled specifiers, passes \`defaultLocalPluginsDir()\` as \`localDir\`, then delegates to SDK \`loadPlugins()\`. Hook failures reported via \`reportHookFailure\` → \`log.error\` + \`captureException\`. Repo's \`plugins/\` directory contains \`audio-transcription/\` and \`example/\` — these are NOT auto-loaded; they must be built and placed in \`\<userData>/plugins/\` or added to the \`plugins\` setting.

<!-- lore:019f2166-b9be-7ab9-a325-5bdb1e224608 -->
* **Local plugin dev workflow: symlink dist/ into userData/plugins/\<slug>/ for live iteration**: For iterative local plugin development outside the main repo, symlink the plugin's \`dist/\` output directory into \`\<userData>/plugins/\<slug>/\` (e.g. \`~/Library/Application Support/Freestyle/plugins/freestyle-voice-my-plugin/\`). \`resolveLocalPackage()\` uses \`fs.existsSync\` + \`fs.readFileSync\` which both follow symlinks, so the loader transparently resolves through the symlink to the live build output. The plugin specifier must still be added to the \`plugins\` setting. Rebuild the plugin (\`pnpm build\`) to pick up changes; server reload (\`POST /reload\`) re-imports the module. This works for plugins developed outside the monorepo — no workspace protocol or monorepo membership required.

<!-- lore:019ee543-2f96-7851-af06-01795ab451f4 -->
* **MCP server tool inventory and gap analysis vs HTTP API**: MCP server at \`apps/server/src/routes/mcp.ts\` (274 lines, mounted \`/mcp\`), server name \`"freestyle"\` v\`"0.0.2"\`. Uses \`StreamableHTTPTransport\`. Tools call \`call(subRouter, method, path, body)\` via \`subRouter.request()\` — uses sub-routers directly, not the top-level app. 15 tools covering formats (CRUD + match + reset), dictionary (CRUD), vocabulary (CRUD), history (list only), cleanup settings (get). \`call()\` takes a Hono sub-router as first arg — MCP is unaffected by the \`createApp\` factory refactor since it references sub-routers directly.

<!-- lore:019f18ae-ce4a-7c71-821a-5dddad989502 -->
* **Onboarding model setup: PR #357 changes silent auto-download to explicit download button**: PR #357 (\`feat/onboarding-model-download-panel\`) replaces silent background local model downloads in onboarding with an explicit \`ModelSetupPanel\` card (new file: \`model-setup-panel.tsx\`, 160 lines). States: Idle (Download button + size), Downloading/building (live progress bar), Error (message + Try again). Panel returns null when model is undefined, not local kind, or status is 'ready'. \`canFinish\` gates tutorial completion for local users until model is ready. Cloud users see the panel as optional (micro-label: 'OPTIONAL — SKIP IF YOU'RE USING CLOUD ONLY'). Main-branch design intent (code comments): 'The user never chooses the model' and 'Background model setup — quiet status, never a decision.'

<!-- lore:019f18ae-ce53-7a82-82f5-299365738007 -->
* **Onboarding VoiceItem/model state: chosen, chosenReady, recommended, RECOMMENDED\_LOCAL\_IDS**: \`chosen = allVoiceItems.find((v) => v.selected) ?? recommended\` (onboarding.tsx line 604). \`chosenReady\`: cloud → \`!!chosen.hasKey\`; local → \`chosen.status === 'ready'\`. \`recommended\` prefers MLX Qwen3 when \`mlxStatus?.canRun === true\`, else Whisper small-q5\_1. Constants: \`RECOMMENDED\_MLX\_DEF = 'qwen3-0.6b-8bit'\`, \`RECOMMENDED\_WHISPER\_DEF = 'small-q5\_1'\`. \`RECOMMENDED\_LOCAL\_IDS = new Set(\['local-mlx/qwen3-0.6b-8bit', 'local-whisper/small-q5\_1'])\` (models.ts lines 247-250, used for badge display). \`VoiceItem.status\` typed as \`'not\_downloaded' | 'downloading' | 'verifying' | 'ready' | 'error'\`. Auto-setup \`useEffect\` guarded by \`autoPicked.current\`; calls \`selectLocalModel\` + optionally \`downloadLocalModel\` + \`commitFreestyleCloudDefault\` for cloud users.

<!-- lore:019f1672-dce4-74ab-a45a-6aa5864a356c -->
* **Pill window is hidden (not destroyed) on hidePill() — WebSocket, AudioContext, Streamer persist between sessions**: \`hidePill()\` (index.ts lines 931–943) calls \`mainWindow.hide()\`, not destroy. The pill window, Streamer singleton, AudioContext, and WebSocket all persist between hotkey sessions. \`streamer.ts\` reuses \`AudioContext\` and worklet if already initialized (\`workletReady\` flag); only loads worklet module on first call. WebSocket reconnects after 1s on close. This means window recreation is never the cause of per-session init delay — the delay comes from \`getUserMedia\` (stream released after each session) and \`prepareSystemAudio\` subprocess.

<!-- lore:019f2173-8a96-7e40-8f3b-e895fb1c264c -->
* **Plugin dev link/unlink script: design and OS-agnostic userData resolution**: Plugin dev link/unlink script lives at \`scripts/link-plugin.mjs\` (repo root). Run via \`pnpm run link\` / \`pnpm run unlink\` from any plugin directory. Strategy: symlink first, fall back to copy. Creates \`\<userData>/plugins/\<slug>-dev/\` (suffix, not prefix) with a generated \`package.json\` (name suffixed \`-dev\`), \`dist/\` symlink → source plugin's dist, \`README.md\` symlink. The \`-dev\` suffix (not \`dev-\` prefix) lets dev and production copies coexist and produces clean display names like 'Profanity Filter Dev'. CRITICAL: symlink alone is insufficient — server won't execute plugin hooks/middleware unless the specifier is in the \`plugins\` setting; developer must enable the plugin in Freestyle UI after linking. Both \`plugins/profanity-filter/package.json\` and \`plugins/audio-transcription/package.json\` have \`link\`/\`unlink\` scripts wired to this shared script.

<!-- lore:019f16bf-54a5-72d3-a0e6-ada0a6000091 -->
* **Plugin discovery: discoverPlugins reads manifests only, never executes plugin code**: Plugin discovery: \`discoverPlugins()\` (manifest.ts) reads \`plugins\` setting, calls \`discoverPackage()\` per entry, scans \`\<userData>/plugins/\` via \`discoverLocalDir()\`. Never executes plugin code — manifest-only. \`discoverPackage()\`: Path A: \`resolvePackageJson()\` via createRequire from 3 base paths; Path B: \`\<userData>/plugins/\<slug>/package.json\`; Path C: neither → \`missingPlugin()\`. npm-installed plugins extracted to \`\<userData>/plugins/\<slug>/\` (NOT node\_modules). \`readManifest()\` extracts name, version, description, author, icon, readme, pages from package.json. Built-in plugins never filtered by disabled lists. \`local: boolean\` field removed from \`DiscoveredPlugin\` and \`PluginInfo\` entirely (PR #353). \*\*Key split\*\*: Electron scans whole directory (safe — manifest-only); server only loads plugins in \`plugins\` setting (security — \`plugins\` setting is explicit allowlist for code execution). \`defaultLocalPluginsDir()\` returns \`null\` when \`FREESTYLE\_DB\_PATH\` unset (remote-server use case).

<!-- lore:019f16c1-29e4-79a1-b9c2-dc8a4fb1c871 -->
* **Plugin installer: atomic staging, npm registry fetch, local-dir materialization**: \`apps/server/src/lib/plugins/installer.ts\` (166 lines). \`resolvePackage()\` queries \`https://registry.npmjs.org/\<name>\`, resolves \`dist-tags.latest\` if no version given. \`installPackage()\`: downloads tarball → verifies integrity (SRI sha512 preferred, falls back to sha1 shasum; throws if no integrity metadata) → stages in \`\<pluginsDir>/.\<slug>-\<random>/\` (same filesystem as dest, NOT OS /tmp) → extracts with \`tar\` (strip:1) → \`fs.rm(dest)\` deletes old version → \`fs.rename(staging, dest)\` atomic move. Never leaves a half-written package — staging cleaned on failure. \`uninstallPackage()\` removes \`\<pluginsDir>/\<slug>/\`. Full install flow: \`installPlugin()\` (loader.ts) → POST \`/api/plugins/install\` → \`installServerPlugin()\` → \`resolvePackage()\` + \`installPackage()\` → \`addSpecifier()\` → \`reloadServerPlugins()\`.

<!-- lore:019f240c-9979-7f3d-9be9-da7dace52f87 -->
* **Plugin LLM access: no direct key access — must use host proxy endpoint or own provider config**: Plugins cannot access the host's \`api\_keys\` table. SDK only exposes global \`settings\` + namespaced plugin storage (\`plugin:\<name>:\<key>\`). Two options for LLM access: (a) plugin stores its own provider+key in plugin settings, (b) call a host proxy endpoint \`POST /api/llm/complete\` (cleaner, reusable across plugins, avoids key duplication). Option (b) requires adding a new route to \`apps/server\`. The \`afterTranscribe\` hook runs in the server process where HTTP calls to \`127.0.0.1:4649\` are available. Plugin cannot reuse the host's configured provider transparently without a host-side proxy.

<!-- lore:019f0340-6c2c-72e2-b0fd-b1151a141616 -->
* **Plugin middleware field: middleware?: MiddlewareHandler\[] on Plugin interface**: The \`Plugin\` interface in \`packages/sdk/src/plugin.ts\` has a \`middleware?: MiddlewareHandler\[]\` field (Hono added as peer dep to \`packages/sdk/package.json\`). \`PluginRegistry\` exposes \`collectMiddleware()\` which returns middleware from all plugins in resolved (enforce-sorted) order. Middleware is collected once at \`startServer()\` time and passed to \`createApp()\` — it is NOT re-evaluated per request. \`Plugin\` is re-exported from the SDK barrel so \`middleware\` is automatically included without barrel changes.

<!-- lore:019f16d8-fe53-71ea-9c0c-c82a06bfaef8 -->
* **Plugin system full flow: UI → IPC → Electron main → HTTP → server → SDK loader**: Full plugin install/uninstall/update-check flow: \`plugins.tsx\` calls \`window.api.installPlugin(npmName, version?)\` → preload IPC \`plugins:install\` → \`ui-host.ts\` handler → \`installPlugin(target, npmName, version)\` (main/plugins/loader.ts, 60s timeout) → POST \`/api/plugins/install\` → \`installServerPlugin()\` → \`resolvePackage()\` + \`installPackage()\` + \`addSpecifier()\` + \`reloadServerPlugins()\`. After install, main calls \`reloadAppPlugins(target)\` then \`refreshPluginUi()\`. IPC channels (ui-host.ts): \`plugins:list\`, \`plugins:refresh\`, \`plugins:set-enabled\`, \`plugins:catalog\`, \`plugins:install\`, \`plugins:uninstall\`, \`plugins:check-updates\`. Server routes: \`POST /reload\`, \`GET /catalog\`, \`POST /install\`, \`POST /uninstall\`, \`POST /check-updates\`. \`setPluginEnabled\` calls server \`/reload\` after toggling disabled\_plugins setting. Update check uses \`semver\` to compare installed vs npm registry latest.

<!-- lore:019f0340-6c2d-76ae-9f62-2a0379b2bad8 -->
* **Plugin system seams: 6 hooks + middleware field, all try/caught in PluginRegistry**: Plugin hooks in \`packages/sdk/src/hooks.ts\`: \`event\` (read-only observer), \`config\` (boot config contribution), \`afterTranscribe\` (raw transcript rewrite), \`beforeCleanup\` (prompt assembly), \`afterCleanup\` (flagship text rewrite — always fires on final text whether or not LLM cleanup ran, same stage as dictionary replacement), \`beforeOutput\` (pre-delivery rewrite). Plus \`middleware?: MiddlewareHandler\[]\` on \`Plugin\` for HTTP-layer injection. All handlers are try/caught in \`PluginRegistry.run()\` — one plugin cannot crash the pipeline; failures route to injected \`onError\` reporter. \`/api/health\` is always exempt from auth (hardcoded in \`EXEMPT\_PATHS\` in \`auth.ts:19\`). Built-in plugins (auth, cloud-sync) are always present and cannot be disabled by users.

<!-- lore:019f16bf-54a8-7390-8d30-a8ebebc63c40 -->
* **Plugin UI hosting: WebContentsView sandboxed in PluginPage, bounds synced via IPC**: Plugin UI hosting: WebContentsView sandboxed in PluginPage, bounds synced via IPC: \`apps/electron/src/renderer/src/pages/plugins/plugin-page.tsx\`. Plugin UI runs in a sandboxed \`WebContentsView\` managed by main process. Renderer reports bounds via \`window.api.showPluginView(slug, pageId, measure(), readTokens())\`. \`ResizeObserver\` + \`window.resize\` keep bounds live via \`window.api.setPluginViewBounds(measure())\`. Unmount calls \`window.api.hidePluginView()\`. 22 CSS design tokens forwarded via \`readTokens()\`. Outer div uses \`WebkitAppRegion: 'drag'\`, inner placeholder uses \`WebkitAppRegion: 'no-drag'\`. Plugin UI CSP: \`connect-src 'self' http://127.0.0.1:\*\`. \*\*View reuse (PR feat/plugin-ui-improvements)\*\*: \`hide()\` detaches the WebContentsView without destroying it; re-opening the same page re-attaches instantly. Only switching to a different plugin page destroys and recreates the view.

<!-- lore:019f16bf-54a4-7fe2-8be9-9d6acfdf8989 -->
* **Plugin UI visibility rules: Open button, local badge, missing badge driven by PluginInfo fields**: Plugin UI visibility rules driven by \`PluginInfo\` fields. \*\*Open button\*\*: rendered only when \`plugin.pages\[0]\` exists; disabled when \`!plugin.enabled\`. \*\*missing badge\*\*: shown when \`plugin.missing === true\`; missing plugins hide Open button. \*\*local badge REMOVED\*\* (PR #353). \*\*Update badge + button\*\* (PR #355): shown when \`PluginUpdateResult.hasUpdate === true\`; clicking triggers reinstall. \*\*Dropdown menu (feat/plugin-ui-improvements)\*\*: Both \`PluginCard\` (plugins.tsx) AND \`PluginDetailPage\` (plugin-detail.tsx) replace the Switch + Trash/Info buttons with a \`MoreHorizontal\` (⋯) dropdown containing: Details (card only), Enable/Disable plugin, separator, Uninstall (destructive, navigates back to \`/plugins\`). Open and Update buttons remain as primary actions outside the menu. \`PluginInfo.pages\` populated by \`parsePluginPages()\` from \`freestyle.contributes.pages\` in \`package.json\`.

<!-- lore:019f16d8-fe52-7bb6-99dd-4b4039ddf546 -->
* **Plugin update mechanism: no dedicated update path — reinstall overwrites atomically**: There is NO dedicated plugin update/upgrade system in the original codebase. Plugin lifecycle had exactly 2 operations: install and uninstall. No version comparison, no update UI, no \`plugins:update\` IPC, no \`/update\` server route. \*\*As of PR #355 (\`feat/plugin-updates\`)\*\*, a full update system was added: \`POST /api/plugins/check-updates\` (uses \`semver\` to compare installed vs npm latest), \`checkForUpdates()\` in loader.ts, \`plugins:check-updates\` IPC, \`checkPluginUpdates\` in preload, \`@tanstack/react-query\` for 1-hour cached update checks in renderer, update badge + button on \`PluginCard\` and \`PluginDetailPage\`. Updating still calls \`installPlugin\` (reinstall) — no separate update code path on the install side.

<!-- lore:019f16fb-4f48-7208-8253-a06d03a6dc44 -->
* **Plugin update-check system: POST /check-updates, semver, react-query 1-hour cache**: \`POST /api/plugins/check-updates\` (added PR #355) accepts \`{plugins: PluginUpdateCheck\[]}\` (array of \`{npmName, installedVersion}\`), queries npm registry for each, uses \`semver.gt(latest, installed)\` to determine \`hasUpdate\`. Returns \`PluginUpdateResult\[]\` (\`{npmName, latestVersion, hasUpdate}\`). Renderer uses \`@tanstack/react-query\` with \`staleTime: ONE\_HOUR\` (3600000ms) and \`retry: 1\`. Query key family: \`\['plugin-updates', entries]\`. \`QueryClientProvider\` wraps \`\<TooltipProvider>\` in \`dashboard.tsx\`. \`checkForUpdates()\` in \`apps/electron/src/main/plugins/loader.ts\` calls via Hono client. IPC channel: \`plugins:check-updates\`. Updating a plugin still calls \`installPlugin\` (reinstall path) — no separate install-side update code path.

<!-- lore:019f1656-0386-71da-8091-9745496cc42f -->
* **PostHog super properties: register() in getClient() for per-event context**: PostHog events get \`app\_version\` and \`environment\` via \`client.register()\` called once in \`getClient()\` (\`apps/server/src/lib/posthog.ts\`). This attaches them as super properties to every event — manual \`capture\`, \`captureException\`, and SDK-autocaptured exceptions — without per-call spreading. \`app\_version\` reads \`process.env.FREESTYLE\_APP\_VERSION ?? 'unknown'\`; \`FREESTYLE\_APP\_VERSION\` is set in \`apps/electron/src/main/index.ts\` via \`app.getVersion()\` alongside \`FREESTYLE\_ENV\`. Per-call \`environment\` spreading in \`capture\`/\`captureException\` was removed as redundant after this change.

<!-- lore:019f16bf-54a1-735b-829e-e0e4cacab8ce -->
* **Profanity Filter plugin: structure, hooks, middleware, and UI**: Profanity Filter plugin at \`plugins/profanity-filter/\`. npm name: \`@freestyle-voice/profanity-filter\`; plugin ID: \`profanity-filter\`. Core: \`src/index.ts\` exports \`profanityFilter(options?)\` — registers \`afterCleanup\` hook and one Hono middleware at \`GET /api/plugins/freestyle-voice-profanity-filter/replacements\`. \`src/replacements.ts\`: 44 default entries, multi-word phrases sorted first, round-trip alt cycling, \`matchCase\` preserves caps/title-case. UI (\`ui/src/App.tsx\`): uses \`@tanstack/react-query\` (useQuery + useMutation) and \`react-hook-form\` — both in \`devDependencies\` (bundled by Vite). \`QueryClientProvider\` wraps app in \`main.tsx\`. Build: \`pkgroll --minify && vite build\`. \`package.json\` \`freestyle\` block: \`icon: 'Sparkles'\`, contributes page \`{id: 'profanity-filter', title: 'Profanity Filter', entry: 'dist/ui/index.html'}\`. Bundle size ~261 kB (gzip ~83 kB) after adding react-query + react-hook-form.

<!-- lore:019f227c-ce9e-734f-bd76-d6f1795ecfa5 -->
* **Root specs/ folder: internal engineering specs and audits (renamed from docs/)**: Root \`specs/\` folder (renamed from \`docs/\` in commit \`b4b8d13\`) contains internal engineering specs and audits written by collaborators as part of Claude Code agent workflows. Structure: \`specs/cloud/support-freestyle-cloud.md\` + 11 files at root including \`freestyle-transcribe-ui-refactor.md\`, \`redesign-models-page.md\`, \`mlx-hub-download-migration.md\`, \`cross-platform-audit.md\`, \`language-setting-audit.md\`, \`transcription-audit.md\`. Pattern: human-written \`# Overview\` + \`# Instructions\` at top, followed by agent-generated technical spec. DISTINCT from \`apps/docs/\` which is the Mintlify product documentation site (user-facing, published). \`AGENTS.md\` documents this distinction. \`CLAUDE.md\` at repo root is a pointer/reference file to \`AGENTS.md\` (not a symlink).

<!-- lore:019f2428-af19-7c78-add5-a93bb2508fd1 -->
* **SDK AfterTranscribeOutput: consumed flag and PluginLlm capability**: \`packages/sdk/src/hooks.ts\`: \`afterTranscribe\` returns \`AfterTranscribeOutput { text: string; consumed?: boolean }\`. When \`consumed: true\`, both \`transcribe.ts\` and \`stream.ts\` skip LLM cleanup and return empty text (triggering \`deliverOutput\` empty-text suppression). \`packages/sdk/src/context.ts\`: \`PluginContext\` has optional \`llm?: PluginLlm\` (\`getModel(): unknown\`, \`providerId: string\`, \`modelId: string\`). \`buildPluginLlm()\` in \`apps/server/src/lib/plugins/context.ts\` constructs it from host providers. ALWAYS guard with \`if (ctx.llm)\` — present only in server mode when default LLM is configured. Plugins never manage their own credentials — LLM access routes through host endpoint only.

<!-- lore:019f0340-6c28-7582-ae9d-21a904706890 -->
* **Server plugin middleware injection: createApp factory pattern**: Plugin middleware is injected at app construction time via \`createApp(pluginMiddleware: MiddlewareHandler\[] = \[])\` in \`apps/server/src/index.ts\`. \`startServer()\` calls \`initServerPlugins({ token })\` first, then \`plugins().collectMiddleware()\`, then \`createApp(pluginMiddleware)\`. Middleware order: \`trustedOrigin → CORS → \[plugin middleware: pre→none→post] → onError → routes\`. Built-in plugins: \`freestyle:auth\` (enforce: pre) and \`freestyle:cloud-sync\` (enforce: post). \`AppType = ReturnType\<typeof createApp>\`. Default export is \`createApp\` factory, NOT an app instance. IMPORTANT: \`reloadServerPlugins()\` only affects hooks — middleware is baked at app construction time and reload does NOT re-inject new middleware. If a plugin's \`dist/index.js\` fails to import (e.g. not built), \`collectMiddleware()\` never collects it and its routes are silently absent.

<!-- lore:019f240c-9973-77a6-b821-f300a6d4bba7 -->
* **Voice Commands plugin: design decisions — hybrid detection, tool-calling agent, LLM proxy**: Voice Commands plugin at \`plugins/voice-commands/\` (\`@freestyle-voice/commands@0.0.1\`). Key decisions: (1) \*\*Detection\*\*: hybrid — cheap deterministic prefilter first, then multi-step tool-calling LLM agent. (2) \*\*Hook point\*\*: \`afterTranscribe\` — fires after STT, before LLM cleanup; empty-text suppression works free via \`deliverOutput\`. (3) \*\*LLM access\*\*: plugin routes through host LLM endpoint via \`PluginLlm\` capability on \`PluginContext\` (added to SDK); plugin never manages own credentials. (4) \*\*consumed flag\*\*: \`AfterTranscribeOutput.consumed?: boolean\` — when true, transcribe.ts/stream.ts skip cleanup and return empty text. (5) \*\*Action types v1\*\*: macOS Shortcuts (macOS-only), HTTP webhook, Open URL/app, Shell command/script. (6) \*\*Platform scope\*\*: cross-platform core, macOS Shortcuts gated. PR #379 (draft) on \`feat/voice-commands-plugin\`.

<!-- lore:019f1672-dce0-7a9d-a3b9-0ffd97bdc276 -->
* **Voice pill audio: startRecording tone/ducking ordering and duckingPromiseRef race fix**: In \`app.tsx\`, \`startRecording\` plays \`playTone('start')\` immediately before ducking (so tone isn't attenuated at \`DUCKED\_VOLUME=0.15\`). Ducking runs concurrently with \`getUserMedia\` via \`duckingPromiseRef.current = prepareSystemAudio(...).catch(()=>{})\`. \`restoreSystemAudioSafely()\` awaits \`duckingPromiseRef.current\` then calls IPC restore — prevents cancel/commit racing a still-in-flight duck subprocess leaving volume stuck at 15%. \`commitRecording\` IIFE: \`await restoreSystemAudioSafely()\` then \`playTone('stop')\`. \*\*Critical guard\*\*: restore is gated on \`duckingPromiseRef.current\` (whether this session actually ducked), NOT on \`\_audioPlaybackMode\` — checking current mode is a regression trap because user can toggle duck→off mid-session, causing commit to skip restore and strand volume at ~15%. All 6 restore call sites route through \`restoreSystemAudioSafely\`.

<!-- lore:019f14f1-8ab1-795c-8f1d-e04dafa02ef7 -->
* **Voice pill programmaticTarget: suppresses spurious move events after setPosition calls**: \`programmaticTarget\` and \`programmaticCleanupTimer\` (declared at \`index.ts:323–324\`) track in-flight programmatic window moves. \`markProgrammaticTarget(x,y)\` sets the target and arms a 1-second safety timeout to clear it in case the OS never delivers a settle event. \`setProgrammaticPosition(win,x,y)\` calls \`win.setPosition()\` then \`markProgrammaticTarget()\`. The \`move\` listener (lines 463–508) ignores events while \`programmaticTarget\` is set, preventing manual-drag saves from firing after \`showPill()\` repositions the window. \`repositionPillForDisplayChange()\` (lines 1677–1689) re-clamps the pill via \`getAppWindowPosition()\` + \`setProgrammaticPosition()\` on \`display-removed\` and \`display-metrics-changed\` events.

<!-- lore:019f14f1-8aa8-75ae-925e-b7684ba88e2f -->
* **Voice pill window positioning: getAppWindowPosition() hardcodes primary display — fix uses cursor screen**: Voice pill (\`mainWindow\`) position is computed by \`getAppWindowPosition()\` at \`apps/electron/src/main/index.ts:363–426\`. Root cause of Issue #318: line 364 hardcodes \`screen.getPrimaryDisplay()\` for all preset positions (top-center, top-right, bottom-right, bottom-center). \`screen.getCursorScreenPoint()\` and \`screen.getDisplayNearestPoint()\` are unused. Fix: replace \`screen.getPrimaryDisplay()\` with \`const cursor = screen.getCursorScreenPoint(); const display = screen.getDisplayNearestPoint(cursor)\` so the pill appears on whichever monitor the mouse is on. \`showPill()\` (lines 697–746) re-calls \`getAppWindowPosition()\` every time the pill is shown, so the fix takes effect per-activation. The \`custom\` case already uses \`screen.getDisplayMatching()\` against saved coords — only the preset cases need updating.

<!-- lore:019ee7ed-0eb5-7c3a-9a59-edca37276c37 -->
* **Winston logger registry: configureAppFileLogging pattern for Electron**: Loggers are created at module import time (before \`app.whenReady()\`), so file transport cannot be added inside \`createAppLogger()\`. Pattern: \`packages/utils/src/logger.ts\` tracks all created loggers in a module-level \`Set\`; \`configureAppFileLogging({ filePath })\` attaches a shared \`winston.transports.File\` retroactively to all existing loggers and to future ones. \`getAppLogFilePath()\` returns the current session log path. Called from \`apps/electron/src/main/index.ts\` after \`app.whenReady()\`. Per-startup filenames: \`freestyle-YYYYMMDD-HHmmss-{pid}.log\` under \`app.getPath('logs')\`. Retain 10 most recent session logs. \`@freestyle/utils\` must NOT import Electron — path is passed in by main.

### Decision

<!-- lore:019ee543-2f99-78f5-9d1f-89c1fe8c085e -->
* **MCP redesign: 15-tool customization-scoped set with vocabulary added and single-item views removed**: Chose 15-tool MCP set over original 11 to cover all customization domains. Changes implemented in \`apps/server/src/routes/mcp.ts\`: ADDED \`vocab\_list\`, \`vocab\_create\`, \`vocab\_update\`, \`vocab\_delete\` (vocabulary was entirely missing); ADDED \`format\_match\` (calls \`GET /match?context=\`, returns matching format or null); ADDED \`cleanup\_get\` (read-only settings fetch). REMOVED \`dict\_view\` and \`format\_view\` — redundant because \`\*\_list\` returns full rows (\`SELECT \*\`). History stays read-only (\`history\_list\` only). Imports added: \`createVocabularySchema\`, \`updateVocabularySchema\` from \`@freestyle/validations\`; vocabulary and settings route imports. MCP tests skipped — tools are thin in-process proxies over HTTP routes already covered by \`api.test.ts\`. Build and all 147 tests pass.

<!-- lore:019efa02-1b7d-796c-99c8-36cd3daccede -->
* **NPM org rename: @freestyle/\* → @freestyle-voice/\*, SDK → freestyle-voice (no scope)**: All monorepo packages renamed from \`@freestyle/\*\` to \`@freestyle-voice/\*\` scope. Exception: \`@freestyle/sdk\` → \`freestyle-voice\` (no scope prefix, standalone package name). Two-pass sed strategy required: pass 1 replaces \`@freestyle/sdk\` → \`freestyle-voice\` first (to avoid being caught by pass 2), then pass 2 replaces remaining \`@freestyle/\` → \`@freestyle-voice/\`. Build artifacts (\`.turbo/\`, \`out/\`, \`dist/\`) must be excluded from sed. \`apps/electron/out/main/index.js\` is a tracked build artifact — skip it. \`PLUGIN\_SCHEME = "freestyle-plugin"\` in \`apps/electron/src/main/plugins/ui.ts\` is a URL protocol, not a package name — leave intact.

<!-- lore:019f18ae-ce55-7e73-bcd6-e02885cdfd17 -->
* **Onboarding cloud UX: silent background download preferred over explicit panel for cloud users**: Main-branch design intent (code comments lines 682, 1252): cloud users 'never had to think about' local model setup — silent background download was the deliberate UX. PR #357 surfaces an optional \`ModelSetupPanel\` for cloud users, which the user flagged as potentially adding unnecessary friction. Preferred resolution: hide \`ModelSetupPanel\` entirely for cloud users (\`chosen.kind === 'cloud'\`) or keep auto-download for cloud users and use the new explicit panel only for local-only users. Chose explicit panel over silent download for local users because silent failures surfaced only as native error dialogs with no in-app recovery path.

<!-- lore:019efae2-43a6-73c7-8bae-eb57025f9ba6 -->
* **Package release versioning: manual explicit semver, OIDC auth, issue-label publish flow**: Chose manual explicit semver (over \`auto\` conventional-commits analysis) for SDK and plugin releases — user selects version explicitly at release time. Chose OIDC trusted publishing (over long-lived \`NPM\_TOKEN\`) for npm auth — requires one-time bootstrap publish per package. Chose reuse of existing issue-label publish flow (over a separate \`workflow\_run\` trigger) for consistency with the Electron app pipeline. \`scripts/bump-version.sh\` only bumps \`apps/electron\` — it is NOT invoked for package releases.

<!-- lore:019efae4-183a-7820-8482-7cf75c8ff499 -->
* **Switched from label-based routing (pkg:\* labels) to title-based routing — chosen because title is authoritative**: switched from label-based routing (pkg:\* labels) to title-based routing — chosen because title is authoritative,

### Gotcha

<!-- lore:019f2291-6849-7663-9bdf-441334b5371f -->
* **.lore.md uncommitted changes block branch checkout — commit on current branch first**: Trap: \`.lore.md\` is auto-updated by the lore system and frequently has uncommitted changes. When switching branches via \`gh pr checkout\` or \`git checkout\`, Git aborts if \`.lore.md\` differs between branches. The repo rules say NEVER stash \`.lore.md\` (it must stay version-controlled). Fix: commit the \`.lore.md\` change on the current branch first (\`git add .lore.md && git commit -m 'chore: update lore'\`), then switch branches. The commit is local-only until explicitly pushed. This is the correct resolution — stashing violates the lore rules.

<!-- lore:019f1407-8c5e-7fe0-8bb6-1323c31da778 -->
* **buildLanguageBlock() returned empty string for auto-detect — LLM translated non-English speech to English**: Trap: \`buildLanguageBlock(undefined)\` and \`buildLanguageBlock('auto')\` both returned \`''\`, so the LLM cleanup prompt had no 'do not translate' constraint for auto-detect sessions. This looks safe because the preset body already contains a weaker 'Keep the output in the same language' rule — but that rule is buried and insufficient to override English-biased LLM behavior. Fix (commit 5db8118): \`buildLanguageBlock()\` now returns \`AUTO\_LANGUAGE\_CONSTRAINT\` for both \`undefined\`/empty and \`'auto'\` cases, explicitly instructing the LLM to preserve source language and script. \`prompts.test.ts\` updated: test renamed from 'returns nothing for auto-detect' to assert the constraint is present.

<!-- lore:019f0348-3332-7e5f-b133-962b77a24f91 -->
* **cloud-sync shouldSync() uses exact path matching, not prefix matching**: Trap: the comment in \`apps/server/src/lib/plugins/builtin/cloud-sync.ts\` \`shouldSync()\` (lines 26-31) previously said 'prefix matching so parameterized routes resolve', which looks correct for REST routes with path params. Fix: the actual implementation uses exact match (\`path === route\`). The comment was misleading — it was updated to reflect exact matching. If prefix/param matching is ever needed, the implementation must be changed, not just the comment.

<!-- lore:019f1507-12d2-751f-9992-27c7faca6415 -->
* **cloudJson throws plain Error with no status — cloud 5xx silently captured as app defect**: Trap: \`cloudJson\` in \`freestyle-cloud.ts\` throws a plain \`Error\` for any non-401 response, carrying no HTTP status. Callers in \`transcribe.ts\` and \`post-process.ts\` then call \`captureException(err)\` unconditionally for non-\`FreestyleCloudAuthError\` errors — so transient upstream 5xx failures (e.g. cloud Worker returning 502) get reported to PostHog as app defects. Fix: introduce \`FreestyleCloudRequestError(status, detail)\` thrown from \`cloudJson\`, add \`isCloudServerError(err)\` helper (returns true when \`status >= 500\`), and gate \`captureException\` at all three cloud catch sites with \`if (!isCloudServerError(err))\`. Analytics \`capture()\` calls and user-facing 500 responses stay unchanged.

<!-- lore:019f1697-9d41-71a8-8c6a-4b0158de1365 -->
* **commitRecording restore guard must use duckingPromiseRef, not \_audioPlaybackMode**: Trap: guarding \`restoreSystemAudioSafely()\` in \`commitRecording\` with \`if (\_audioPlaybackMode !== 'off')\` looks correct — why restore if ducking is disabled? But \`\_audioPlaybackMode\` is mutable module state; if the user toggles duck/pause → off \*during\* an active recording, the commit skips restore and strands system volume at ~15% (\`DUCKED\_VOLUME\`). Fix: gate on \`duckingPromiseRef.current\` instead — truthy only when this specific session actually started ducking. \`restoreSystemAudioSafely\` already awaits the ref before calling IPC restore, so the guard is simply \`if (duckingPromiseRef.current)\`. Fixed in commit \`eaf9c07\`.

<!-- lore:019efae2-43a2-72d9-b1d1-b36349845d80 -->
* **Craft npm workspace mode bumps ALL packages to same version — cannot use for independent versioning**: Trap: Craft's npm target supports \`workspaces: true\` with \`includeWorkspaces\`/\`excludeWorkspaces\` filters, which looks like it enables per-package independent versioning from a single root \`.craft.yml\`. Fix: workspace mode still publishes all included packages at the SAME version under a single release pipeline. For truly independent versions, each package needs its own \`.craft.yml\` in its subdirectory and a separate workflow using the \`path\` input. Craft source even has a \`// TODO support multiple configuration files\` comment confirming this is a known limitation.

<!-- lore:019eda7f-9b5d-78fb-8329-83f90a0468dc -->
* **Electron loads @freestyle/server from compiled dist, not TS source — server changes require rebuild + restart**: Trap: editing \`apps/server/\*\*\` TypeScript looks like it should take effect immediately (like renderer hot-reload), but Electron resolves \`@freestyle/server\` via package \`exports\` to \`dist/index.js\` — no \`--conditions=development\` flag is passed, so Node never uses the TS source. Fix: after any server-side change, run \`turbo build\` (or rebuild the server package) then fully quit and restart the Electron app. Renderer changes hot-reload; server changes do not. Symptom: DB has correct values, source code looks right, but behavior matches old logic — stale dist is the culprit.

<!-- lore:019ee7ed-0ebc-7190-b1ab-2752e75f2a02 -->
* **Export route refactor: renderer callers must be updated when route shape changes to POST**: Trap: after changing export endpoints from \`GET /export/json\` to \`POST /export\` with body \`{ type }\`, the renderer pages (\`dictionary.tsx\`, \`vocabulary.tsx\`) still call the old \`$get()\` RPC shape — TypeScript catches this only in \`typecheck:web\`, not during normal dev. Fix: update renderer export callers to use \`POST /export\` with \`{ type }\` request body. Always run \`typecheck:web\` after route shape changes to catch stale RPC callers in the renderer.

<!-- lore:019f1697-9d46-7703-82a6-67f628a78aaa -->
* **git rebase --continue hangs when git launches interactive editor — use GIT\_EDITOR=true**: Trap: \`git rebase --continue\` after resolving conflicts launches \`$EDITOR\` to confirm the commit message, blocking indefinitely in a non-interactive agent context — looks like a hang with no output. Fix: prefix with \`GIT\_EDITOR=true\` (\`GIT\_EDITOR=true git rebase --continue\`) to accept the default commit message without opening an editor. Also: \`.lore.md\` may be re-modified by a background process during the timeout, requiring re-staging (\`git add .lore.md\`) before rebase can continue.

<!-- lore:019f0348-3334-7e1e-9748-87f2d7bbec74 -->
* **loadPlugins() JSDoc omitted built-ins — builtins are always prepended first**: Trap: \`loadPlugins()\` JSDoc in \`packages/sdk/src/loader.ts\` (lines 121-123) described load order as 'entries → local files → flatten presets → setup → sort by enforce', which looks complete. Fix: built-in plugins (passed via \`LoadPluginsOptions.builtin\`) are prepended BEFORE all other plugins, then the enforce-sort runs. The JSDoc was updated to include this. Built-ins bypass npm import/factory flow but \`setup()\` is still called. Built-ins are never filtered by disabled lists.

<!-- lore:019efa1d-9985-7ced-b16a-3d5a576ca69e -->
* **macOS 'representedObject is not a WeakPtrToElectronMenuModelAsNSObject' console noise is benign**: Trap: bursts of \`representedObject is not a WeakPtrToElectronMenuModelAsNSObject\` in Electron console during text input look like a bug caused by recent code changes (e.g. npm org rename). Fix: this is macOS AppKit re-validating Edit menu items (undo/redo/cut/copy/paste/selectAll roles at \`apps/electron/src/main/index.ts:1328-1339\`) when a text input is focused or typed in — each validated item emits one log line. \`rebuildMenus()\` is only called at startup and on update-downloaded, never on keystrokes. Safe to ignore in dev. To silence: set \`OS\_ACTIVITY\_MODE=disable\` env var on the dev script, or filter shell output with grep.

<!-- lore:019f227e-a37e-7f26-9576-59c9779f2191 -->
* **Mintlify lockup SVGs from cloud repo require cloud CSS — use self-contained SVGs instead**: Trap: \`lockup-dark.svg\` / \`lockup-light.svg\` from the cloud repo look like drop-in logo assets. They rely on CSS classes (\`class="wm"\`, \`class="dot"\`) for text styling and won't render standalone without the cloud app's CSS. Fix: create self-contained SVGs that embed font loading (Google Fonts \`@import\`) and inline all styles. The cloud app's wordmark uses \*\*Instrument Serif\*\* italic (\`--font-instrument-serif\`), waveform polyline from \`components/Logo.tsx\` at \`viewBox="0 0 100 100"\`. Existing docs PNGs (\`logo/freestyle-\*.png\`) were byte-for-byte identical to cloud repo PNGs — pixelation was from Mintlify navbar scaling, not low resolution. SVGs solve this with infinite sharpness.

<!-- lore:019f18ae-ce50-763c-b239-f62da7e67da8 -->
* **ModelSetupPanel localSetupModel fallback shows recommended model after switching to cloud**: Trap: \`localSetupModel\` in PR #357 onboarding.tsx falls back to \`recommended\` when no local model is selected. If a user picks a local model then switches to cloud, \`chosen.kind === 'cloud'\` but \`localSetupModel\` still resolves to the recommended local model (MLX Qwen / Whisper small) — the panel continues to show a download prompt for a model the user didn't choose. Fix: gate \`localSetupModel\` on \`chosen.kind !== 'cloud'\` (or hide \`ModelSetupPanel\` entirely when \`chosen.kind === 'cloud'\`). Identified as a low-to-medium severity bug in PR #357 review.

<!-- lore:019efa02-1b7f-7631-a7a2-c2cbc71c686f -->
* **pluginSlug() test assertion must be updated manually after scope rename**: Trap: \`packages/sdk/src/ui.test.ts\` line 6-8 asserts \`pluginSlug('@freestyle/plugin-audio-transcription')\` → \`'freestyle-plugin-audio-transcription'\`. After renaming to \`@freestyle-voice/\`, bulk sed updates the input string but NOT the expected output string — test fails silently with wrong assertion. Fix: manually update expected value to \`'freestyle-voice-plugin-audio-transcription'\`. Similarly, \`loader.test.ts\` fixture folder names (e.g. \`freestyle-plugin-x\`) must match the slug derived from the new scoped name (\`freestyle-voice-plugin-x\`) — sed renames the specifier but not the fixture directory name.

<!-- lore:019f1656-0384-7808-ad79-cd854cfd83e9 -->
* **posthog-node isServer option doesn't exist in v5.35.8 — causes type error**: Trap: PostHog docs mention an \`isServer\` option for \`PostHogOptions\`, which looks like a useful server-side flag. Fix: \`isServer\` does not exist in \`posthog-node@5.35.8\` — it's neither in the types nor at runtime (a newer release feature). Adding it causes \`TS2353: Object literal may only specify known properties\`. Drop it rather than bumping the dependency. Use \`client.register({ app\_version, environment })\` as super properties instead — this is the correct mechanism for attaching context to every event (manual capture, captureException, and SDK autocapture).

<!-- lore:019f2148-0e4e-753b-969a-3aa796a6062e -->
* **Profanity filter plugin UI loads but API 404s — middleware not collected because plugin wasn't built**: Trap: The profanity filter plugin UI HTML loads successfully (protocol handler works, files exist on disk), so the page renders — but \`useReplacements()\` gets a 404 on \`GET /api/plugins/freestyle-voice-profanity-filter/replacements\`. This looks like a server routing bug. Root cause: the plugin's \`dist/index.js\` was never built, so \`loadPlugins()\` can't import it, \`collectMiddleware()\` never collects its Hono middleware, and the route is never mounted. Fix: run \`pnpm build\` in the plugin package before installing/testing. The UI assets (\`dist/ui/\`) can exist independently of \`dist/index.js\` — UI files don't prove the server-side plugin loaded. Also applies to the dev link workflow: after \`pnpm run link\`, the plugin must be built AND enabled in Freestyle UI before server-side hooks/routes are active.

<!-- lore:019f0a67-5008-7f15-ae01-1c4ba5df277b -->
* **Provider logo invisible on selected filter pill — dark:invert-0 removes inversion against primary bg**: Trap: \`ProviderAvatar\` used \`dark:group-data-\[variant=default]/button:invert-0\` to 'restore' logos when a pill is selected, which looks correct (undo inversion for selected state). Fix: in dark mode, \`dark:invert\` makes SVG logos white; when selected (\`bg-primary\` is dark), removing the inversion makes the dark logo invisible. The fix is to keep \`dark:invert\` always and drop \`dark:group-data-\[variant=default]/button:invert-0\` entirely — logos stay white on both selected and unselected pills. File: \`apps/electron/src/renderer/src/components/model-row.tsx\` line 37.

<!-- lore:019f1672-dce1-747e-8b78-ee1da7f0cb31 -->
* **releaseStream() after every session forces getUserMedia round-trip — init delay for cloud providers**: Trap: \`releaseStream()\` is called after every recording session (in \`cancelRecording\`, \`app.tsx:891\`), stopping all mic tracks so the OS mic indicator turns off. This looks correct for privacy, but forces a fresh \`getUserMedia()\` round-trip on every hotkey press — the dominant init delay for cloud providers (no local Whisper). Fix: user confirmed preference to keep this behavior (release stream after each dictation). The delay is mitigated by running \`prepareSystemAudio\` concurrently with \`acquireStream()\` rather than sequentially. \`recorder.ts:acquireStream()\` reuses existing stream if tracks are live — but since \`releaseStream()\` nulls the stream, it always falls through to \`getUserMedia\`.

<!-- lore:019ee7ed-0eb9-76ec-b7e7-b2c64eea40f5 -->
* **SegmentedControl (Radix ToggleGroup) fires empty string on re-click — must guard onValueChange**: Trap: Radix \`ToggleGroup type='single'\` fires \`onValueChange('')\` when the active item is re-clicked, which looks like a valid deselection and can clear state. Fix: guard with \`onValueChange={(v) => v && onValueChange(v)}\` in \`segmented-control.tsx:39\`. The component is designed to always keep a selection, so the empty-string case should always be suppressed. This is a recurring cross-session directive.

<!-- lore:019f1337-b901-7bc1-afba-c5594112d007 -->
* **SelectContent position='item-aligned' breaks dark theme bg-popover on Windows**: Trap: \`SelectContent\` in \`apps/electron/src/renderer/src/components/ui/select.tsx:60\` defaulted to \`position='item-aligned'\` (non-upstream shadcn). In item-aligned mode, Radix wraps the listbox in extra positioning elements that don't carry \`bg-popover\`, so the dark theme list background doesn't render — list appears light while text stays dark-colored. Fix: change default to \`position='popper'\` (upstream shadcn default) — content renders directly in the styled \`Content\` element so \`bg-popover\` covers the full surface. Introduced in commit \`cb6ba80\` (#298 UI unification refactor), not a deliberate UX choice. All \`\<SelectContent>\` in \`settings.tsx\` and \`language-selector.tsx\` inherit this default.

<!-- lore:019f0340-6c29-7aef-ba9a-fcc1fd8ed21f -->
* **Server default export is createApp factory — test files must call createApp()**: Trap: \`apps/server/src/index.ts\` default export was previously an app instance; it is now the \`createApp\` factory function. Test files that do \`import app from '../src/index.js'\` and call \`app.request(...)\` will get \`TypeError: app.request is not a function\`. Fix: call \`createApp()\` to get an app instance. Auth tests specifically need \`createApp(\[authMiddleware])\` to enable bearer token enforcement — without passing \`authMiddleware\`, the test app has no auth and all requests return 200.

<!-- lore:019f1672-dce4-74ab-a45a-6aa4cce2fada -->
* **Vertical/dockless monitor: bottomOverlap=14 clips pill at screen edge**: Trap: \`bottomOverlap = 14\` in \`getAppWindowPosition()\` (index.ts line 378) pushes bottom-positioned pill 14px past \`workArea\` bottom. On primary display with a dock, \`workArea.bottom < physical bottom\` so the overlap is hidden behind the dock — looks correct. On secondary/vertical monitors with no dock, \`workArea.bottom == physical bottom\` → pill is clipped ~6px below screen edge. Fix: compute \`bottomInset = display.bounds.y + display.bounds.height - (display.workArea.y + display.workArea.height)\`; only apply \`bottomOverlap\` when \`bottomInset < 14\` (i.e., dock is present and large enough to absorb it).

### Pattern

<!-- lore:019eda79-a6cf-7977-ab47-e17f20c8f6fd -->
* **Custom prompt save: explicit Save button pattern (not debounced autosave)**: The cleanup custom prompt uses an explicit Save button instead of debounced autosave. \`use-models.ts\`: \`setCleanupCustomPrompt\` only updates local state; \`saveCleanupCustomPrompt()\` does the \`PUT /api/settings/cleanup\_custom\_prompt\` and toggles \`savingCustomPrompt\`. \`customPromptDirty\` is derived (current text ≠ last saved baseline). \`cleanup-intensity.tsx\`: Save button (\`variant="ink" size="sm"\`) shown only in Custom mode. Three states: saving (Loader2 spinner), dirty (enabled), clean (disabled). Rationale: debounced autosave had an unmount-flush gap — navigating away within 500ms silently dropped the last edit because \`clearTimeout\` on unmount cancelled the pending PUT without flushing it.

<!-- lore:019ee7ed-0ebd-7843-8f23-cf0806351761 -->
* **Diagnostics log redaction rules**: Never log: transcript text, clipboard contents, API keys, bearer tokens, custom prompts, full request bodies, raw audio paths, or full settings dumps. Safe to log: app version, platform/arch, packaged vs dev mode, model/provider IDs (not keys), durations, HTTP status codes, error names/messages/stacks, process exit codes, logs path, server mode. Apply \`winston.format.errors({ stack: true })\` for Error serialization. These rules apply to both file logs and PostHog properties.

<!-- lore:019ee3de-f95c-7a46-aa90-2f6dee5ba4dc -->
* **Export endpoints: POST /export with body { type } pattern**: Export endpoints use \`POST /export\` with JSON body \`{ type: 'json' }\`. Both dictionary (\`POST /api/dictionary/export\`) and vocabulary (\`POST /api/vocabulary/export\`) follow this pattern. Validated via \`exportSchema\` from \`packages/validations/src/export.ts\`: \`EXPORT\_TYPES = \['json'] as const\`. Routes use \`switch (type)\` with 400 fallback. Vocabulary also has \`POST /import\` using \`INSERT OR IGNORE\`, returning \`{imported, skipped}\`. Dictionary import similarly exists at \`dictionary.ts:155\`.

<!-- lore:019f0a67-500c-775e-8b11-ab15f23581b4 -->
* **Filter pill (Chip) left padding reduction when logo present — pl-1 override**: When a \`Chip\` in the model filter bar has a provider logo (\`mark\` prop), the default \`px-2\` (8px) Button padding makes the avatar look too far from the pill edge. Pattern: add \`cn('gap-1.5 rounded-full', mark && 'pl-1')\` to reduce left padding to 4px only when a logo is present. File: \`apps/electron/src/renderer/src/pages/models/model-list.tsx\` line 641. \`cn\` is imported from \`@renderer/lib/utils\`.

<!-- lore:019ee543-2f9d-7bbf-927f-c9d108c2928e -->
* **format\_match endpoint: context-aware format selection logic**: \`GET /api/formats/match?context=\<string>\` at \`formats.ts:72\`. Does case-insensitive substring match on pipe-delimited \`app\_pattern\` field. User rules (\`is\_default=0\`) take priority over defaults (\`is\_default=1\`). Returns matching row or \`null\`. \`POST /api/formats/reset\` at \`formats.ts:133\` deletes all non-default rules (\`is\_default=0\`), restoring factory state. \`format\_match\` MCP tool added in \`mcp.ts\` — calls this endpoint with \`{ context: z.string() }\` input.

<!-- lore:019f0ebe-4531-783a-afe9-70a20376c163 -->
* **Local plugin dev workflow: two methods for testing unpublished plugins**: Two ways to test a local plugin without npm publish: 1. \*\*Single file drop\*\*: place \`.ts\`/\`.js\`/\`.mjs\` into \`\<userData>/plugins/\` — auto-discovered by \`discoverLocalPlugins()\`. 2. \*\*Package folder\*\*: place full package at \`\<userData>/plugins/\<slug>/\`. Loader resolves entry via \`package.json#main\` using \`resolveLocalPackage()\` — symlinks to local \`dist/\` work. Plugin name must be added to \`plugins\` setting for server to execute hooks/middleware. \`defaultLocalPluginsDir()\` returns \`null\` when \`FREESTYLE\_DB\_PATH\` is unset. \*\*Architecture split reason\*\*: server only loads explicitly enabled plugins (security — \`plugins\` setting is allowlist); Electron scans whole \`\<userData>/plugins/\` directory manifest-only (never executes code). Link/unlink script at \`scripts/link-plugin.mjs\`. Plugin bridge guard: always check \`window.freestyle\` before use — page can be opened outside host during dev.

<!-- lore:019f227e-a380-7c1d-a7df-da4bb6ee61ee -->
* **Mintlify navbar social links: use type 'github'/'discord' for icon-only display with live counts**: In \`docs.json\`, \`navbar.links\[].type: 'github'\` shows repo star count (no label needed); \`type: 'discord'\` shows online user count. Both render as icon-only when \`label\` is omitted. \`navbar.primary\` supports \`type: 'button' | 'github' | 'discord'\`. Top-level navigation groups always expand and cannot be collapsed (Mintlify platform constraint). \`navigation.global.anchors\` adds persistent sidebar items across all pages. \`contextual.options\` supports built-in values: \`add-mcp\`, \`aistudio\`, \`assistant\`, \`copy\`, \`view\`, \`chatgpt\`, \`claude\`, \`cursor\`, \`devin\`, \`mcp\`, \`perplexity\`, \`vscode\`, etc.

<!-- lore:019f2224-433c-78c2-b16c-5f4082a034bf -->
* **Plugin bridge guard: always check window.freestyle before use in plugin UI**: Plugin UI pages must always guard for the bridge before use: \`const bridge: FreestyleBridge | undefined = window.freestyle; if (!bridge) throw new Error(...)\` (or handle gracefully). Rationale: plugin pages can be opened directly in a browser during development where \`window.freestyle\` is undefined. This is a documented directive in \`first-plugin.mdx\` and a cross-session recurring instruction. Never assume \`window.freestyle\` is present — always guard first.

<!-- lore:019f21b8-36e5-7bda-9839-a72f26830700 -->
* **Plugin UI back button: always max-content width, not full-width**: Plugin UI back buttons (\`.back-btn\`) must use \`width: max-content\` (or \`display: inline-flex\`) so they don't stretch full-width. Full-width back buttons look wrong — they appear as a wide bar rather than a compact nav element. Both \`profanity-filter\` and \`audio-transcription\` plugins follow this pattern. The back button navigates via \`window.freestyle?.invoke('navigate', { to: '/plugins' })\`. Audio-transcription back button lives in \`ui/index.html\` with click handler in \`ui/src/main.ts\`.

<!-- lore:019f21b8-36e4-7af2-ba6e-131eed25fb83 -->
* **Plugin UI top padding: host provides 28px spacer — don't double-pad in plugin CSS**: The Electron plugin host (\`plugin-page.tsx\`) renders a \`h-7\` (28px) div above the plugin WebContentsView placeholder. Plugin CSS \`.page\` should NOT add its own large top padding — doing so doubles the gap. Correct pattern: \`padding: 8px 48px 48px\` (small top) or \`padding-top: 0\` if the host spacer is sufficient. App page spacing reference (\`globals.css\` \`.responsive-page-scroll\`): \`padding-inline: 3rem\`, \`padding-bottom: 3rem\` (default); scales down at ≤1080px and ≤820px; NO top padding — top spacing comes entirely from the host's \`h-7\` spacer.

<!-- lore:019f1717-b1b5-7bf6-8eee-179ce068dd53 -->
* **React Query migration pattern: useQuery replaces useState+useEffect+useCallback, useMutation replaces manual fetch+reload**: Standard migration pattern used across formats.tsx, today.tsx, dictionary.tsx, vocabulary.tsx, history.tsx, plugins.tsx, plugin-detail.tsx, use-cloud-usage.ts: Replace \`useState\`+\`useCallback\`+\`useEffect\` fetch pattern with \`useQuery({ queryKey: \[...], queryFn, staleTime: ONE\_HOUR, retry: 1 })\`. Replace mutation functions that call \`loadData()\` after with \`useMutation\` + \`queryClient.invalidateQueries\`. IPC subscriptions (\`onTranscriptionDone\`) trigger \`queryClient.invalidateQueries\` instead of calling a callback. Paginated queries include \`\[page, search]\` in query key so pagination/search changes auto-refetch. \`enabled\` option used for conditional fetches (e.g. \`enabled: signedIn\` in use-cloud-usage.ts).

<!-- lore:019ee7ed-0ebb-763c-9008-83099d099f8a -->
* **SegmentedControl wrap prop: 2-column grid for overflow on narrow screens**: When a \`SegmentedControl\` has 4+ options with long labels (e.g. widget position), the default \`w-fit flex-row\` with \`shrink-0\` items overflows on narrow screens. Solution: \`wrap\` prop on \`SegmentedControl\` switches container to a 2-column grid below \`min-\[1360px]\`, reverting to single flex row at \`min-\[1360px]\` and wider. Breakpoint is 1360px (not 1280px) because the right column in the settings layout is too tight at 1280px. Thread \`wrap\` through any local wrapper (e.g. \`Segment\` in \`settings.tsx\`) and enable only on the affected control.

<!-- lore:019eda8f-2bf5-7af9-b71b-9d32dc243ebc -->
* **Settings route key-specific validation pattern**: In \`settings.ts\` route, each settings key has its own validation branch: \`cleanup\_intensity\` is validated via \`cleanupIntensitySchema.safeParse\` (returns 400 \`"Invalid cleanup intensity"\` on failure); \`cleanup\_custom\_prompt\` via \`cleanupCustomPromptSchema.safeParse\` (returns 400 \`"Custom prompt is too long"\` on failure). Schemas live in \`@freestyle/validations/settings\`. \`parseCleanupIntensity()\` coerces arbitrary persisted values to a valid \`CleanupIntensity\` with fallback to \`DEFAULT\_CLEANUP\_INTENSITY\` — use this when reading from DB to handle stale/invalid stored values.

<!-- lore:019ee543-2f9b-73f2-bdd2-2d2956e0d4ca -->
* **Vocabulary vs Dictionary domain distinction in Freestyle**: Dictionary (\`key\`→\`value\`): post-transcription exact string replacement. Vocabulary (\`term\`+\`notes\`): pre-transcription ASR recognition bias — hints the speech model to recognize specific terms. These are distinct pipelines. Schemas in \`packages/validations/src/\`: \`createDictionarySchema\` requires \`key\`+\`value\` (min 1); \`createVocabularySchema\` requires \`term\`, optional \`notes\`. Constants: \`VOCABULARY\_TERM\_MAX=200\`, \`VOCABULARY\_NOTES\_MAX=2000\`, \`VOCABULARY\_IMPORT\_MAX=1000\`. \`GET /vocabulary/all\` returns terms ordered by length DESC (used for ASR injection). Duplicate term on \`POST /vocabulary/\` returns 409.

### Preference

<!-- lore:019f2164-6fb0-7f5a-ac0b-4b58eddf7dc6 -->
* **Always annotate key behavioral invariants with 'never' statements tied to specific file locations**: When exploring a codebase, the user explicitly calls out hard behavioral guarantees using 'never' phrasing (e.g., 'never executes plugin code', 'never filtered by disabled lists', 'never crashes plugin discovery', 'never leaves a half-written package'). These are always tied to a specific file and line number. When summarizing code behavior, mirror this pattern: extract and surface absolute invariants as 'never' assertions with precise file:line citations, not just general descriptions. These invariants are treated as load-bearing facts worth highlighting separately from ordinary behavior descriptions.

<!-- lore:019ef818-39f0-7dc6-b613-b7d367f2d30a -->
* **Always ask investigative clarifying questions before making changes**: Before modifying anything, the user asks targeted read-only investigation questions to understand the current state of the codebase (e.g., 'should X be part of Y?', 'where is Z used?', 'should this file be removed?'). They expect the assistant to trace through actual files, line numbers, git state, and architecture to confirm or deny assumptions — and only then propose concrete actions. The user does not want premature changes; they want evidence-based answers first, with explicit confirmation before any modification is made.

<!-- lore:019f18ae-cee7-715a-992d-cc46ca53e68a -->
* **Always assert design intent via code comments as authoritative constraints**: When the user references code comments (e.g., 'The user never chooses the model', 'Background model setup — never a decision'), treat those comments as authoritative statements of design intent that override or constrain PR review suggestions. The user embeds UX philosophy directly in source comments and expects the assistant to recognize these as binding constraints when evaluating changes. If a PR deviates from a pattern documented in a code comment, flag it as a regression against stated intent rather than a neutral trade-off. Similarly, when the user verbally asserts a behavioral rule (e.g., 'cloud users never had to think about it'), treat it as equivalent to a documented design requirement.

<!-- lore:019ef9fd-9140-79fc-93a6-321828890475 -->
* **Always audit and plan before executing bulk refactors**: Before making bulk changes (renames, refactors, migrations), the user consistently performs a thorough discovery phase first: grepping for all affected files, reading key files to understand edge cases, identifying exceptions (build artifacts, test fixtures, special cases), and establishing a numbered step-by-step execution plan with ordering dependencies noted. Only after this full audit does execution begin. When assisting, always map the full scope of impact, call out exceptions and gotchas explicitly, and present a sequenced task list before touching any files.

<!-- lore:019f14f3-5fcd-7734-870a-1d873352caef -->
* **Always batch multiple GitHub issues into a single PR creation request**: When working on GitHub issues, the user groups multiple related issues together and asks the assistant to create PRs for all of them in one request (e.g., 'create PRs resolving issues #201, #202, #318'). Prior to requesting PRs, the user typically conducts thorough read-only investigations of each issue. The assistant should handle all requested issues in the same session, creating separate PRs per issue (or combined where appropriate), without requiring the user to repeat the request issue-by-issue.

<!-- lore:019f2211-f15a-78ec-91ef-e38504705364 -->
* **Always begin sessions with thorough parallel exploration of the monorepo structure before making changes**: At the start of each session, the user consistently requests a comprehensive exploration of the freestyle monorepo before any implementation work. This includes: root package.json/turbo.json/pnpm configs, all apps/ and packages/ directories, plugin-related files, CI/workflow files in .github/, and any relevant subsystem (auth, server, SDK, etc.). The user expects parallel/simultaneous file reads across multiple directories, exact file paths, and structural summaries. This is a research-first pattern — map the codebase thoroughly before writing any code. Always run broad parallel exploration tools upfront rather than exploring incrementally.

<!-- lore:019f21b8-36d0-7824-b991-330164a106dc -->
* **Always build and commit after completing UI or code changes**: After making code or UI changes, the user consistently runs the build (typically \`pkgroll --minify && vite build\` or turbo build), then immediately commits and pushes to the current feature branch with a descriptive conventional commit message (e.g., \`fix:\`, \`feat:\`). The commit message summarizes what changed at a functional level. This pattern applies after any meaningful change — UI polish, bug fixes, new features, or script fixes. The assistant should expect that after implementing changes, the next steps are: build, verify output, commit with a conventional message, and push.

<!-- lore:019f2145-c420-7435-9036-907c5ca3d70a -->
* **Always clarify the exact nature of a bug before accepting the assistant's interpretation**: When the assistant begins investigating a bug, the user consistently corrects or narrows the framing before accepting the investigation. Specifically, the user distinguishes between superficially similar errors (e.g., API 404 vs. page 404, 'server returned 404' vs. navigation 404) and redirects the assistant to the precise failure point. Follow-up questions from the user often reframe the root question entirely (e.g., 'does this plugin even have a UI?' rather than 'why is the UI broken?'). When investigating bugs, always confirm the exact failure mode and layer (network, routing, rendering, navigation) before proceeding, and present the clarifying question explicitly rather than assuming.

<!-- lore:019f0a63-264e-7e1f-ad5d-25f593a93ba0 -->
* **Always clarify UI bug location precisely when initial fix targets wrong element**: When a UI fix is applied to the wrong element (e.g., outer wrapper instead of inner component), the user immediately clarifies the exact element and property that needs fixing, often describing it in terms of visual structure ('inside the pill', 'the logo area') rather than code. The user expects the assistant to re-investigate the correct component and apply a targeted fix. Always confirm the precise element (e.g., internal padding of a Chip vs. its wrapper div) before applying layout/spacing fixes, especially when provider logos or nested UI components are involved.

<!-- lore:019f1703-8ca2-7df3-8a63-dd9b715c1978 -->
* **Always combine code review/refactor tasks with a final 'mark ready for review' step**: When the user assigns a multi-part coding task, they consistently structure it as: (1) do the substantive work (find patterns, apply refactors, fix bugs, DRY violations, etc.), followed by (2) a final verification/build step, and (3) explicitly marking the PR ready for review. The assistant should treat 'mark ready for review' as a mandatory closing step, not optional. Task lists should always include a build/verify step before the PR marking step. The user also tends to batch related migration or cleanup work into the same session rather than doing it piecemeal.

<!-- lore:019f21c8-b0ea-70a0-8892-cdc885a4b749 -->
* **Always commit and push after completing each discrete fix or feature**: After completing a logical unit of work (a bug fix, UI polish, refactor, or new feature), the user immediately commits with a descriptive conventional commit message and pushes to the remote branch. Commits are granular — one concern per commit — and messages follow the format \`type: description\` (e.g., \`fix: polish profanity filter UI\`, \`feat: add plugin storage API\`). The assistant should proactively commit and push after each completed task without waiting to be asked, using an appropriate conventional commit type (feat, fix, refactor, chore) and a concise imperative description.

<!-- lore:019f0333-261d-7d1f-a431-49fa6062f484 -->
* **Always conduct exhaustive read-only research before any code changes**: Before implementing any feature or making edits, the user consistently runs a complete read-only exploration phase: reading relevant source files, confirming interfaces, tracing data flows, and mapping the full codebase structure. This research phase is explicitly completed and logged before any edits begin. When planning new features (e.g., a CLI tool), the user wants the agent to fully understand existing patterns, file structures, SDK contracts, and conventions first. Do not skip or abbreviate the research phase — read all relevant files, confirm types and interfaces, and document findings before proposing or making changes.

<!-- lore:019f227a-108c-7dfc-ae05-d1a3e7593ec1 -->
* **Always consult official documentation before implementing platform-specific changes**: When making changes that involve third-party platforms or tools (e.g., Mintlify, Craft, SDK APIs), the user expects the agent to fetch and review the official documentation first before implementing anything. This applies especially when the platform has constraints or configuration options that aren't obvious (e.g., Mintlify navbar icon config, Craft's single-pipeline limitation). The agent should proactively identify relevant docs pages, extract key constraints and available options, and note any platform limitations that affect the implementation approach — before writing any code or config changes.

<!-- lore:019f16c9-6c61-7ac9-8c6f-43b316d3ac2e -->
* **Always create a dedicated branch, commit, and open a PR for changes rather than committing directly to main**: When changes are ready to land, the user consistently creates a new feature/fix branch off an up-to-date \`main\`, commits the changes there with a conventional commit message, and then opens a PR for review before merging. They do not push directly to \`main\` for non-trivial changes. The branch name follows a \`type/short-description\` convention (e.g., \`fix/remove-plugin-local-badge\`, \`feat/plugin-ui\`). After the PR is opened, a self-review or thorough review is performed, bugs are fixed, and the PR is squash-merged. Always follow this branch → commit → PR → review → squash-merge workflow.

<!-- lore:019f2152-95a1-7033-b33c-53a55bddafaa -->
* **Always create a new branch before implementing any changes**: When the user has a set of changes to implement, they consistently require a new Git branch to be created before any code modifications begin. This applies even when multiple changes are grouped together — the branch must be created first, then implementation follows. If the assistant starts making changes without first creating a branch, the user will stop and insist on the branch being created. Always ask for or confirm a branch name and create it before touching any files.

<!-- lore:019f16f1-3f23-7bfd-ab46-b8834aec9b47 -->
* **Always create a numbered task plan before implementing multi-step features**: When tackling a feature spanning multiple files/systems, the user consistently creates an explicit numbered task list upfront (e.g., 13 tasks for plugin update-check), ordered by dependency, with each task scoped to a single file or concern. Tasks cover the full stack: backend routes, shared types, main process logic, IPC wiring, preload layer, UI components, i18n keys, and build verification. A 14th self-review task is also added. The plan is created before any code is written; tasks are executed and marked complete sequentially. The assistant should propose a numbered task plan first, get implicit or explicit approval, then execute tasks one by one in order.

<!-- lore:019f2181-4748-733b-be31-bacaea651657 -->
* **Always design plugin dev tooling as portable scripts bundled with example plugins**: When designing developer workflows for the plugin system, the user consistently wants link/unlink scripts to be self-contained and shipped directly with example plugins and templates (not as a separate global tool). Scripts should live in \`package.json\` as \`pnpm run link\` / \`pnpm run unlink\`, handle OS-agnostic userData path resolution (macOS/Linux/Windows), use symlinks with copy fallback on Windows, and optionally call the running server's API. The long-term intent is for these scripts to be scaffolded by a \`create-freestyle-plugin\` CLI. Always design for this bundled-with-example pattern rather than a global install approach.

<!-- lore:019f228c-63d9-7bd2-b9e6-91834e6027d4 -->
* **Always document codebase internals as structured bullet observations with emoji severity markers**: The user consistently records detailed codebase exploration findings as bullet-point observations prefixed with emoji severity markers: 🔴 for critical/important details, 🟡 for informational notes. Each observation includes timestamps, file paths, constants, interfaces, function signatures, and behavioral notes. This appears to be a systematic knowledge-capture pattern used across sessions to build a persistent understanding of the codebase. When exploring code, expect the user to want thorough, precise documentation of what was found — including exact values, types, file locations, and behavioral rules — formatted in this structured observation style.

<!-- lore:019f1663-4aa1-7022-a1e9-a40c01875269 -->
* **Always document root cause hypotheses and design intent inline during code review**: During code review sessions, the user consistently records not just what code does, but also root cause hypotheses for bugs (e.g., 'move listener recomputes position using cursor on wrong display → spurious custom save'), design intent extracted from code comments (e.g., 'never block app shutdown on audio restore failure'), and architectural findings (e.g., 'window is hidden not destroyed, so WebSocket persists'). These observations are marked with emoji indicators (🟡 for findings, 🔴 for user directives/preferences). When assisting, surface root cause reasoning explicitly alongside code descriptions, and treat code comments as authoritative statements of design intent worth highlighting.

<!-- lore:019ee7f8-0b5d-738e-83a0-42a1407e90a6 -->
* **Always ensure errors and analytics failures are silently swallowed and never interrupt the UI or app flow**: The user consistently enforces a pattern where all error handling, analytics, and logging code must be strictly fire-and-forget. Errors must never propagate to interrupt the UI or crash the app. This applies to: analytics calls (wrap in try/catch or .catch(() => {})), PostHog capture calls, renderer error forwarding, and log file writes. The user has explicitly stated 'failures never interrupt the UI' and 'Never let analytics errors affect the app' as hard rules. When implementing any error reporting, telemetry, or logging feature, always wrap in silent try/catch blocks, chain .catch(() => {}) on promises, and ensure the happy path continues regardless of whether the diagnostic/analytics code succeeds or fails.

<!-- lore:019f16d4-d969-72e6-9130-69e7f05c37bf -->
* **Always expect plan/read-only mode exploration before implementation begins**: The user consistently initiates or confirms a plan/read-only investigation phase before any code changes are made. When starting a new feature or continuing work, the user expects the assistant to first explore the existing codebase architecture, understand current patterns, and form a complete plan — without making edits. The user also asks clarifying questions during this phase (e.g., about UI behavior, existing features, plugin structure) to validate understanding. Only after this exploration and confirmation does implementation proceed. Always enter plan mode first, read relevant files, and surface findings before proposing or writing any code.

<!-- lore:019ee447-edae-7ea1-a049-c50eefb083f0 -->
* **Always explore and map existing architecture before proposing solutions**: When asked to design or build something new (e.g., a plugin system, endpoint summary), the user expects the assistant to first enter a read-only exploration mode — systematically reading directory layouts, entry points, route files, middleware, and existing patterns — before proposing any design or making changes. The assistant should enumerate concrete file paths, framework details, and architectural decisions already in place, then use those findings to inform the proposal. Never jump straight to a solution without first grounding it in the actual codebase structure.

<!-- lore:019f16b8-ed7b-701f-8bee-e28274a8f4d6 -->
* **Always explore and map the codebase thoroughly before making changes**: Before implementing any feature, refactor, or change, the user consistently requests a thorough exploration of the relevant codebase area first. This includes locating: plugin manifests/configs, entry points, UI files, server-side catalog entries, route definitions, SDK infrastructure, and any related test files. The user wants a complete architectural map — with specific file paths and line numbers — covering all relevant seams before any code is written or modified. Follow this pattern by proactively reading and reporting file locations, key exports, constants, and structural relationships before proposing or making changes.

<!-- lore:019f238b-2705-7285-9afc-15b7e0db4ee5 -->
* **Always explore and understand existing architecture before designing new features**: When starting a new feature or plugin, the user consistently expects the assistant to enter a read-only/plan mode first and thoroughly explore the existing codebase — understanding architecture, plugin system, hooks, manifests, and example implementations — before proposing any design or writing any code. The user often arrives with an idea that is partially formed (e.g., unsure about timing or representation), and expects the assistant to ground the design in what already exists rather than proposing something abstract. Do not jump to implementation; always audit the relevant existing code first.

<!-- lore:019ee427-7657-7453-8cf0-1c3635d3e294 -->
* **Always explore codebase architecture before writing any code**: When starting work on a feature or investigation, the user consistently performs exhaustive read-only exploration of all relevant files before making any changes. They read full file contents (with line counts), trace cross-file dependencies, identify all seams and integration points, and build a complete mental model of the system. Sessions are often purely research-oriented with no edits made. The assistant should prioritize thorough file reading, confirm exact line numbers and content, and map out the full architecture before proposing or writing any code.

<!-- lore:019f16d7-252e-7700-b73b-01f627a58ad7 -->
* **Always explore codebase architecture by requesting specific file contents with precise line ranges**: When investigating how a system works, the user consistently requests exact file contents with specific line ranges (e.g., 'lines 98–end', 'lines 260–290') rather than asking for summaries or high-level explanations. They trace full flows across multiple files (UI → IPC → main process → server → installer), requesting surrounding context (e.g., '10 lines of surrounding context') around key symbols. They focus on confirming architectural facts (e.g., 'no update mechanism exists', 'atomic rename pattern', 'never touches DB') by reading actual code. When answering questions, provide precise file paths, line numbers, function signatures, and exact field names rather than paraphrased descriptions.

<!-- lore:019f1c5e-d786-7af2-8d0d-83b0601a3c81 -->
* **Always explore codebase architecture thoroughly before drafting external communications about it**: When the user wants to write tweets, blog posts, or other external content about a technical feature (e.g., the plugin system), they first conduct or request deep codebase exploration — reading source files, interfaces, hooks, manifests, and examples — before composing any public-facing text. The assistant should support this by proactively surfacing accurate implementation details (type signatures, file paths, hook names, conventions) so the communication reflects the real architecture rather than approximations. Don't draft the tweet/post until the relevant code has been examined.

<!-- lore:019f0330-db37-7374-8614-579065d167a1 -->
* **Always explore codebase exhaustively before proposing or implementing changes**: When starting work on a feature, investigation, or question about the freestyle codebase, the user consistently requests broad, multi-file exploration first — reading full file contents with line numbers, searching across directories, and mapping out all related files — before any implementation or answer is given. This applies to new topics (plugin system, auth, storage, UI) and revisited topics alike. The user wants exact file paths, line numbers, and summaries for each finding. Always perform thorough upfront reconnaissance across all relevant files in packages/, apps/, and plugins/ directories rather than answering from partial knowledge or jumping straight to implementation.

<!-- lore:019f0a37-0f4b-7948-acf1-53e5d1758229 -->
* **Always explore codebase structure before making changes or answering questions**: When asked about how a system works (e.g., plugin discovery, install flow, flag values), the user expects thorough file-by-file tracing through the actual source code — reading specific line ranges, following call chains across multiple files, and confirming exact values/logic before giving an answer. The user wants precise citations (file path + line numbers) for every claim. They do not accept high-level summaries without grounding in the actual implementation. This applies especially to questions about data flow, flag semantics, and feature behavior in the freestyle-voice codebase.

<!-- lore:019f21c7-c707-7449-88a0-c313e3b2f073 -->
* **Always explore the freestyle plugin system architecture before making changes**: When starting a new session or task in the freestyle-voice monorepo, the user consistently requests a thorough exploration of the plugin system architecture before any implementation work begins. This includes examining: the plugins/ directory structure, packages/sdk/ source files, apps/electron/src/main/plugins/ and apps/server/src/lib/plugins/ internals, plugin package.json manifests (especially the 'freestyle' key), and relevant SDK types (Plugin, PluginContext, hooks). Always proactively read and summarize these files at session start when working on plugin-related tasks, rather than waiting to be asked.

<!-- lore:019eea7c-a6d3-7dd9-a1d9-6706895e6d8e -->
* **Always follow recommended options when planning questions are asked**: When the assistant presents planning questions with labeled options (e.g., 'Recommended'), the user consistently selects the recommended option without deviation. When implementing features, follow the recommended path directly rather than offering alternatives. This applies to architectural decisions (e.g., where to place UI sections, how much detail to show), component placement, and feature scope. If a recommended option exists, treat it as the user's default preference and proceed with it.

<!-- lore:019f2163-867d-71f7-81b1-b82998fe9101 -->
* **Always front-load research with specific named systems and search queries before implementation**: When exploring a new technical pattern or architecture decision, the user specifies exact real-world reference systems to study (e.g., VSCode, Obsidian, npm, pnpm) AND provides explicit web search queries to run. This happens before any implementation work begins. The user wants comparative analysis of how established tools solve the problem, not just a direct answer. Follow this by researching all named systems, running the specified searches, and synthesizing findings into patterns before proposing any solution for their specific codebase.

<!-- lore:019ee53c-504d-7914-807b-03e9edf96284 -->
* **Always investigate codebase structure through systematic read-only exploration before making changes**: The user consistently performs thorough read-only reconnaissance of the codebase before making any modifications. They map out file structures, route definitions, line numbers, tool counts, and gaps between what exists vs. what is exposed. This pattern applies when evaluating features, APIs, or integrations — the user wants a complete inventory first (what exists, what's missing, what's unexposed) before deciding what to build or change. When helping this user, proactively provide exhaustive structural details: file paths, line numbers, method signatures, and explicit gap analysis comparing what's implemented vs. what's accessible.

<!-- lore:019ee53a-f18c-7b0e-8460-0ffe07f8c81b -->
* **Always investigate existing architecture before proposing or implementing changes**: When starting work on a feature or investigation, the user consistently begins with a thorough read-only exploration of the codebase — mapping routes, file structure, middleware, and existing patterns — before any modifications or proposals. This applies to tasks like auditing endpoints, planning plugin systems, or comparing MCP vs. API coverage. The user expects the assistant to enumerate specifics (file paths, line numbers, HTTP methods, handler summaries) during this discovery phase rather than jumping to implementation. Do not suggest changes or designs until the architectural landscape is fully mapped.

<!-- lore:019f16d2-8ff3-7ecd-87e3-323a5f0d7402 -->
* **Always investigate existing codebase before assuming features exist**: When asked about a feature or system, the user expects a thorough investigation of the actual codebase to determine whether the feature exists at all, rather than assuming it does. The user wants concrete findings: which files were checked, what routes/functions/interfaces exist, and explicit confirmation when something is absent. The user accepts and values 'this feature does not exist' as a complete, valid answer. Responses should cite specific file paths and line numbers to support findings, and clearly distinguish between what exists vs. what is missing.

<!-- lore:019f16bd-81b7-70b9-8f41-36f971ad476f -->
* **Always investigate plugin system internals before implementing or confirming behavior**: When working with the plugin system, the user consistently probes specific mechanics before proceeding: which fields drive UI behavior (\`PluginInfo.pages\`, \`local\`, \`missing\`), how dev-mode loading works, what testing options exist without npm publish. Key invariants to always verify from source: (1) \`parsePluginPages\` never crashes — drops invalid entries; (2) \`discoverPlugins\` never executes plugin code — manifest-only; (3) Open button visibility driven by \`plugin.pages\[0]\` existence; (4) \`local\` badge from \`plugin.local === true\`; (5) plugin slug derived by \`pluginSlug()\` in \`packages/sdk/src/ui.ts\`. When answering plugin questions, look up loader, catalog, manifest, and UI components and cite specific line numbers.

<!-- lore:019f14f1-8b24-7b72-9f7f-83b3bfaa21ac -->
* **Always investigate root cause thoroughly before making any code changes**: When approaching bugs or issues, the user consistently requests (or expects) a complete read-only investigation phase first — identifying exact file paths, line numbers, root causes, and a fix plan — before any code is modified. The user wants precise, evidence-based diagnosis (e.g., 'I need to be precise about the most likely root cause') with specific code references. Only after the investigation is complete and the root cause is confirmed does implementation proceed. Never jump straight to editing code; always explore and document the problem space first.

<!-- lore:019f2192-37b6-7a33-80b4-dee85a8f21cb -->
* **Always investigate root cause through systematic file/code verification before accepting explanations**: When debugging plugin or system issues, the user consistently drills down through each layer of the stack by reading actual file contents, running verification commands, and checking real values (slugs, paths, realpaths, dedup logic) rather than accepting theoretical explanations. They expect the assistant to trace the exact code path — discovery → manifest parsing → UI rendering — and confirm or rule out each hypothesis with concrete evidence (file sizes, timestamps, boolean comparisons). Don't speculate; read the relevant source lines and verify with filesystem checks.

<!-- lore:019f1664-ace0-7ecc-bef3-206787daabd1 -->
* **Always investigate root causes through systematic code review before proposing fixes**: When bugs are reported, the user expects a thorough code review process that traces the exact root cause through specific file paths, line numbers, and code logic before any fix is proposed. The user documents intermediate findings (hypothesis → confirmation) and expects the assistant to read and cite actual code (line numbers, variable names, values) rather than guessing. Bug investigations follow a pattern: reproduce the mechanism, identify the triggering condition, confirm with code evidence, then fix. The user treats unconfirmed hypotheses as distinct from confirmed root causes and marks them differently (e.g., 🟡 vs 🔴).

<!-- lore:019f14f7-9598-7bd1-99d5-ba9b29d1b576 -->
* **Always link PostHog error tracking issues when reporting bugs for investigation**: When reporting a new bug or crash, the user consistently provides a PostHog error tracking URL alongside the error message and stack trace. This applies when requesting investigation and fixes for runtime errors. The assistant should treat the PostHog link as part of the bug report context, acknowledge it, and use the error details (stack trace, middleware chain, affected table/module) to guide root cause analysis. The user also expects fixes to be added to the existing open PR rather than creating a new one, when a related PR is already in progress.

<!-- lore:019f13f6-9c8c-7219-aa9a-82d978b936a8 -->
* **Always mark PRs ready for review after all CI checks pass**: After implementing a fix and verifying that all CI checks pass, the user expects the PR to be marked ready for review (transitioning from draft to open). This applies when: (1) the PR was created as a draft, (2) all CI checks have concluded with 'success', and (3) a self-review of the diff confirms the fix is correct. Use \`gh pr ready \<number>\` to mark it ready. Do not leave a draft PR in draft state once CI is green and the fix has been validated.

<!-- lore:019f1697-9dc0-739d-9666-73e84e755085 -->
* **Always perform a code review after creating a PR and fix any bugs found before the session ends**: After pushing a branch and creating a PR, the user consistently requests a full code review of the diff. During the review, the user expects the assistant to identify real bugs (not just style issues), explain root causes clearly, and immediately apply fixes. Fixes are committed in the same session, typically as fixup commits referencing the original commit. The review covers correctness, race conditions, and edge cases — not formatting. The session is not considered complete until the bug fix is committed and verified (lint + typecheck clean).

<!-- lore:019f2291-6948-7775-86a2-1f12aa2deb92 -->
* **Always perform code reviews by fetching full PR metadata, diff, and related issue context before giving feedback**: When the user asks for a PR review, they expect the assistant to gather complete context first: PR metadata (title, author, branch, state, files changed), the full diff, the linked issue description, and any relevant pre-existing code (e.g., via grep). Only after collecting all this context should the assistant provide a thorough review that identifies bugs, regressions, or design concerns — including edge cases like race conditions, mode-toggle regressions, or caller-side implications. The review should also note any open decisions left by the PR author (e.g., 'switch to audio file instead').

<!-- lore:019f21f6-7dbf-7b2a-927c-a947edac40e2 -->
* **Always perform thorough read-only exploration before making any code changes**: Before writing or editing any code, the user consistently reads and maps out the full relevant codebase: directory structures, key files, interfaces, types, and implementation details. This exploration phase is explicitly read-only and comprehensive — covering multiple layers (SDK, apps, plugins, scripts, configs, CI). The user expects the assistant to enumerate files, summarize their contents with line-level precision, and build a complete mental model of the system before proposing or making any changes. Only after this research phase does the user proceed to tasks like code review, planning, or implementation.

<!-- lore:019f2290-09fa-7564-871d-36fef95aac4a -->
* **Always post PR reviews directly to GitHub with a conversational, human tone**: When the user asks for a PR review, they expect the assistant to post the review directly to GitHub via the API (not just summarize it in chat). Reviews should be written in a conversational, human tone — not overly formal or bullet-point heavy. The review should include an overall positive/constructive summary and targeted inline comments on specific lines where relevant. The state should be COMMENTED (not APPROVE or REQUEST\_CHANGES) unless explicitly told otherwise. Inline comments should flag concrete issues (e.g., side-effects of the change on other systems) with actionable suggestions.

<!-- lore:019f0a67-506a-7222-a21b-1fb68c9c10ff -->
* **Always push fixes directly to main with a single descriptive commit after completing a set of related changes**: After completing a set of related UI or code fixes (often 2–5 small edits across files), the user commits all changes together in a single commit pushed directly to main, bypassing PRs and CI checks. Commit messages follow conventional commit format (e.g., \`fix: \<short description of what was fixed\`). The user does not open pull requests, does not wait for CI, and does not split fixes into separate commits per file. Group related fixes into one atomic commit with a concise conventional-commit message summarizing the overall intent.

<!-- lore:019ee7f6-abb8-7b39-ad06-b67f43ab403b -->
* **Always read and confirm exact file contents before implementing changes**: Before implementing any changes, the user consistently requests full file reads with line numbers to confirm exact contents, imports, interfaces, component structure, and API calls. This applies to all files that will be modified or that are dependencies of the change. The user expects the assistant to report back specific details: line counts, import lists, exported interfaces/types, function signatures, and i18n keys in use. Only after this confirmation step does the user proceed with implementation. Never assume file contents from prior context — always verify current state first.

<!-- lore:019f241c-5743-7fdc-9765-b70b2010a873 -->
* **Always read and document existing codebase structure before making changes**: Before implementing any new feature or making edits, the user consistently performs a thorough read-only research phase: reading key source files, documenting interfaces, types, hook signatures, and architectural patterns. This applies especially when working in an unfamiliar area of the codebase. The user expects the assistant to explore and record relevant file contents (interfaces, types, method signatures, constants) before writing any code. Research findings are treated as ground truth for the implementation that follows.

<!-- lore:019f2419-1d48-7f64-8ffd-f2bca5dc16cc -->
* **Always read and explore existing code/architecture before making changes**: Before implementing any feature, the user expects the assistant to enter a read-only/planning mode first — inspecting relevant existing files, patterns, and architecture — before proposing or executing any changes. This applies to new features, PRs, CLI tools, and refactors alike. The assistant should explicitly state it is in exploration/planning mode, identify the exact files and patterns relevant to the task, and only then proceed with implementation. Skipping this reconnaissance phase is not acceptable, even for seemingly straightforward tasks.

<!-- lore:019f16ec-33da-742c-813e-a3b6d738b494 -->
* **Always read multiple related files in parallel when exploring a codebase area**: When the user needs to understand a feature, system, or area of the codebase, they request multiple related files to be read simultaneously (in parallel) rather than one at a time. They typically identify 3-7 files upfront that together form a complete picture (e.g., entry points, providers, routes, or a full feature stack across layers). The assistant should batch all file reads into a single parallel operation rather than reading sequentially and reporting incrementally. When initial searches return no results, broaden the search scope before reporting failure.

<!-- lore:019f0a57-9b89-73ce-83b9-f38895cb7b79 -->
* **Always report UI bugs via screenshots and expect targeted, minimal fixes**: The user consistently reports UI layout or visual bugs by sharing screenshots, then expects the assistant to investigate the specific files involved, identify the root cause precisely, and apply the smallest possible fix (e.g., removing a single CSS class, changing one padding value). The user does not want broad refactors or over-engineered solutions. When a fix overshoots (too much spacing, wrong visual result), the user explicitly says so and expects a revert to the original plus a minimal targeted adjustment. The assistant should read relevant files, pinpoint the exact line causing the issue, apply a surgical change, and confirm the edit succeeded.

<!-- lore:019f21ee-3cf8-7834-8720-586981c5ea57 -->
* **Always request a full PR diff review for bugs, DRY violations, and inconsistencies before marking ready for review**: When a PR is nearing completion or CI is failing, the user consistently asks the assistant to perform a comprehensive review of all PR changes — covering bugs, DRY violations, and inconsistencies — before marking the PR as ready for review. This applies both proactively (user-initiated pre-merge review) and reactively (CI failure triggers a review). The assistant should systematically scan the full diff, flag repeated code patterns, consolidate constants or logic where applicable, verify CI status, and only mark the PR ready after all issues are resolved.

<!-- lore:019f140f-c99f-7d23-9ea1-379b4854e30c -->
* **Always request a self-review for bugs, DRY issues, and code quality before marking a PR ready for review**: After committing changes and pushing a branch, the user consistently requests both PR creation and an immediate self-review in the same step (or as the very next action). The review should be thorough: examine the full diff, identify bugs, regressions, or missing edge cases, and apply fixes before the PR is considered ready. This pattern applies to all PR types regardless of size. The assistant should proactively perform the review without waiting to be asked separately, and commit any fixes found during review directly to the branch.

<!-- lore:019ee3b4-39a4-7c1d-ae7e-aeffab221800 -->
* **Always request comprehensive endpoint/route documentation with method, path, handler summary, and file location**: When exploring a server codebase, user consistently requests a full inventory of all HTTP endpoints. Each entry must include: HTTP method, full mounted path (including prefix), concise handler summary (what it does, key behaviors, validation, side effects), and source file path with line numbers. Must cover every router and sub-router, not just top-level routes. This is a recurring session-start orientation pattern. Freestyle project: Hono-based server at \`apps/server/src/\`; main router mounts \`apiRouter\` at \`/api\`, \`mcp\` at \`/mcp\`, \`stream\` at \`/stream\`. Sub-routers under \`/api\`: settings, keys, models, transcribe, history, dictionary, vocabulary, formats, post-process, whisper, mlx-asr. Export endpoints: \`POST /api/dictionary/export\` and \`POST /api/vocabulary/export\` (body \`{ type: 'json' }\`).

<!-- lore:019f21f8-c427-7a2a-be67-70cd2932d9ec -->
* **Always request full code review of PR diffs before making changes**: When working with PRs or branches, the user consistently reads the full diff across all changed files before taking any action (implementing, fixing, or committing). They load multiple relevant files upfront (4–7+ files spanning all layers: UI, Electron main, server routes, shared types, schemas) to build complete context. Only after this full review do they identify bugs, DRY violations, naming inconsistencies, or correctness issues — then fix and commit. Follow this pattern: always read all changed files in a PR/branch before proposing or making any changes.

<!-- lore:019f2152-94e9-7b81-b276-aa2cc71281b9 -->
* **Always request full file contents with line numbers when exploring unfamiliar code areas**: When starting to investigate a feature, bug, or system (e.g., plugin UI, onboarding flow, plugin loading), the user consistently requests full file contents with line numbers for all relevant files upfront — often 4–11 files at once — rather than asking for summaries or partial excerpts. This applies at the start of a new investigation session. The user identifies specific files by path and wants them read in parallel. Follow this pattern by reading all requested files completely with line numbers, not truncating or summarizing unless explicitly asked.

<!-- lore:019f21fb-0e74-7afe-a68e-c12c78f439cc -->
* **Always request full PR review with bug fixes before merging**: When working on a feature branch, the user consistently requests a thorough code review of all branch changes before merging — covering bugs, DRY violations, inconsistencies, and missing edge cases — and expects the reviewer to also fix any issues found, not just report them. Reviews are scoped to the diff between the feature branch and main/merge-base. The user provides or expects identification of: changed files, review task breakdown by area, and resolution of all findings in the same session. This pattern applies to both small PRs and large ones (50+ files).

<!-- lore:019efae2-443c-7c33-8614-8366cca5baf4 -->
* **Always request local-only changes before committing or pushing**: When starting a session that involves file edits or new file creation, the user explicitly requests that all changes be made locally first for review before any git commit or push occurs. Apply all edits and file creations to the working directory only. Do not run \`git add\`, \`git commit\`, \`git push\`, or any equivalent operation unless the user explicitly approves after reviewing the local changes. This applies to all file types: config files, workflow YAML, package.json edits, scripts, etc.

<!-- lore:019f16d6-af9d-7588-92b7-5ea8f26164a7 -->
* **Always request multiple relevant files upfront before implementation**: Before any implementation or planning work, the user consistently requests the full contents (with line numbers) of multiple related files in a single batch — often 4–7 files at once. These files span the relevant layers of the codebase (e.g., renderer pages, main process modules, shared utilities, routes, preload). The user does this to establish full context before changes are made, rather than reading files incrementally. When assisting, proactively identify and read all relevant files in parallel at the start of a task, rather than reading one file at a time or asking the user which files to inspect.

<!-- lore:019ee643-62b7-711f-855a-94cdc4a116ee -->
* **Always request read-only architectural audits with exact file paths and line numbers before making changes**: When exploring or auditing a codebase (API endpoints, MCP tools, routes, plugins, Dockerfiles, etc.), the user consistently requires precise source locations: exact file paths and line numbers for every finding. This applies to read-only investigations, architecture mapping, and capability inventories. When providing any structural analysis — route listings, tool registrations, endpoint descriptions, configuration details — always include the specific file path (e.g., apps/server/src/routes/mcp.ts) and line number(s) where each item is defined. Never give vague or approximate references. Organize findings by category (e.g., HTTP method, full path with prefix, description, source location) as the user expects structured, comprehensive inventories rather than summaries.

<!-- lore:019f21e6-e991-7369-b3e4-09b8f48c900c -->
* **Always request thorough code review with specific focus areas before merging branches**: When working on feature branches, the user requests comprehensive code reviews comparing the branch to main before merging. Reviews consistently cover: (1) bugs — logic errors, race conditions, null checks, type issues; (2) DRY violations — duplicate code across files; (3) inconsistencies — naming mismatches, missing error handling, hardcoded values; (4) missing edge cases — empty inputs, special characters, boundary conditions. The user also provides context about which duplications are intentional (e.g., standalone templates). When conducting such reviews, cover all changed files systematically and flag issues in each category explicitly.

<!-- lore:019f1703-8c87-7813-90aa-d41f8432d67b -->
* **Always request thorough file audits with structured output before starting migrations**: Before beginning a migration or refactor effort, the user consistently requests a comprehensive scan/audit of all relevant files in a directory. They define specific patterns to search for, want results organized per-file with metadata (file path, line numbers, pattern description, classification, complexity rating), and want results ranked or prioritized. This applies to React Query migrations, plugin system changes, and similar cross-cutting refactors. When responding, proactively read all relevant files in parallel, classify findings against the user's defined criteria, and present structured output with rankings before proposing any implementation plan.

<!-- lore:019f2141-3023-758a-b80f-43a759f5439e -->
* **Always request thorough, multi-step codebase investigations with explicit trace paths**: When debugging an issue, the user consistently asks for a comprehensive, multi-area investigation rather than a quick fix. They enumerate specific areas to investigate (e.g., 7 numbered targets) covering the full stack: manifest/config files, UI components, routing, build output, IPC/protocol handlers, and server endpoints. They want the full flow traced end-to-end before any solution is proposed. Follow this by systematically reading all relevant files across the codebase in the order specified, reporting findings per area before drawing conclusions.

<!-- lore:019f2163-8690-71d9-9dbb-10aee9bf7c0f -->
* **Always research existing codebase before proposing or implementing solutions**: When facing a problem or new feature, the user consistently asks the assistant to first investigate the existing codebase (tracing flows, reading specific files with line numbers) and/or research external approaches before any implementation begins. The user explicitly requests a read-only/plan-mode phase. This applies to debugging (tracing button flows, plugin loading), architecture decisions (local dev workflows, update systems), and feature design. The assistant should always start with thorough investigation — reading relevant files, mapping data flows, identifying what already exists — before suggesting or writing any code.

<!-- lore:019f21a2-3f68-7886-8595-276da7f3b905 -->
* **Always research external ecosystem patterns before designing internal APIs**: When designing or evaluating a plugin/extension system feature (storage, dev mode, update systems, etc.), the user consistently requests research into how multiple established ecosystems solve the same problem before making implementation decisions. They name specific systems to survey (VSCode, Obsidian, Figma, Chrome, Grafana, WordPress, npm, pnpm, etc.) and specific APIs or patterns to investigate within each. This pattern applies when the user is about to add a new capability to their Freestyle plugin SDK — they want a comparative landscape of prior art first, then will synthesize findings into their own design. Always perform the requested cross-ecosystem research thoroughly before proposing any implementation.

<!-- lore:019f21ac-5007-7d6f-93fe-8e0699a864e1 -->
* **Always research prior art before designing new systems**: When designing a new system, API, or feature (especially infrastructure like plugin storage), the user explicitly asks the agent to research how other established systems solve the same problem before proposing a design. This applies to plugin systems, storage APIs, configuration patterns, etc. The user expects a survey of multiple real-world examples (typically 4–6 systems), extracting concrete API shapes, naming conventions, and design tradeoffs, before any implementation proposal is made. The research should inform the design rather than the agent jumping straight to a solution.

<!-- lore:019f1403-e29a-79ab-85be-fcc28d76a731 -->
* **Always resolve GitHub issues by following the resolve-issue skill workflow end-to-end**: When given a GitHub issue to resolve, the user expects the agent to follow a structured workflow: read the issue → check for existing PRs → explore the codebase → investigate root cause → plan → implement → verify → deslop → review → commit with \`Fixes #\<number>\` → open a draft PR. The user consistently uses git worktrees at \`~/dev/\<owner>/\<repo>-wt/\<branch>\`, never force-pushes to others' branches, and never runs \`git reset --hard\` or \`git clean -fd\` in the main clone. The user expects thorough root cause investigation before implementing any fix.

<!-- lore:019f21d9-a1a7-79d6-8e50-4158c845c682 -->
* **Always review before committing — explicitly cancel or defer commit tasks**: When an agent plans to commit code as part of a multi-step task, the user consistently intervenes to stop the commit and review the changes first. The user does not want automatic commits at the end of build/implementation tasks. Always pause before any \`git commit\` step and explicitly ask the user if they want to review before committing. Never include a commit step as an automatic final action in a task plan without user confirmation.

<!-- lore:019efafc-604a-739a-ba04-85e032af1108 -->
* **Always review changes locally before committing and pushing**: After making code changes, the user consistently runs a full build (including typecheck, native compilation, and electron-vite build) and confirms it succeeds before committing and pushing to the remote branch. The build verification step is treated as a mandatory gate — commits only happen after all tasks pass (typecheck:node, typecheck:web, compile:native, electron-vite build). The user also expects Biome formatting/linting to run as part of the pre-commit hook (nano-staged), and the final commit message follows conventional commit format (e.g., 'feat:', 'fix:'). This pattern applies consistently across feature work, bug fixes, and refactors.

<!-- lore:019efb13-4149-7d17-8879-b43011672489 -->
* **Always review, fix bugs/DRY issues, then squash-merge PRs**: When the user asks to handle a PR, they consistently follow a three-phase workflow: (1) thoroughly review all changed files for bugs, DRY violations, and inconsistencies, (2) fix any issues found before merging, and (3) squash-merge the PR into main. The user expects the assistant to proactively identify and fix problems (not just report them), verify merge state is CLEAN and CI passes before merging, and complete the full cycle in one session. This applies to PRs of any size.

<!-- lore:019f1670-913d-7836-948e-abcd7127c07b -->
* **Always run build, typecheck, and lint checks before committing**: After implementing any fix or feature, the user consistently runs the full verification suite before committing: build (e.g., \`electron-vite build\` or \`turbo build\`), TypeScript typecheck (\`tsc --noEmit\`), and Biome lint/format (\`biome check --write\` or \`biome format --write\`). If Biome reformats files, those changes are included in the commit. Only after all checks pass clean does the user proceed to stage and commit. This applies to both small targeted fixes and larger multi-file changes.

<!-- lore:019f0340-6c74-736a-ae2a-371f46392090 -->
* **Always run builds and tests to verify changes before considering work complete**: After implementing any code changes, the user consistently runs the full build pipeline and test suite to verify correctness before marking tasks done. This includes: running package builds (e.g., \`pnpm build\` for SDK and server), running all test files and checking for 0 failures, and fixing any test failures that arise from the changes (e.g., updating test imports or middleware setup). The user treats a clean build + all-green tests as the explicit completion criterion for a task. When tests fail, the user expects immediate diagnosis and fixes, followed by a re-run to confirm 0 failures.

<!-- lore:019f220b-8850-7bdc-ac8a-da6b0f57974e -->
* **Always run full review then fix all issues before committing**: When working on a PR or branch, the user consistently follows a two-phase pattern: (1) conduct a thorough review covering bugs, DRY violations, naming inconsistencies, and type errors across all affected files, then (2) fix every identified issue before committing and pushing. The user expects all typechecks, builds, and lint checks to pass before the commit is made. Minor issues flagged during review are not deferred — they are resolved in the same session. Commits are only made after all checks pass cleanly.

<!-- lore:019f2213-c5da-7ac3-b72f-cef51751ea8e -->
* **Always start new features with thorough codebase exploration before making changes**: When beginning any new task — whether adding a new app, migrating content, building a CLI tool, or implementing a feature — the user consistently requests exhaustive exploration of the existing codebase first. This includes reading full file contents with line numbers, mapping directory structures, understanding existing patterns, and researching external references before any edits are made. The assistant should enter a plan/read-only phase, gather all relevant context in parallel where possible, and present findings before proposing or executing changes. Never jump straight to implementation.

<!-- lore:019f21fd-5954-7688-847d-611a1b2833f6 -->
* **Always summarize completed work across all implementation layers before starting new tasks**: At the start of sessions (or after confirming prior tasks are done), the user expects a comprehensive summary of what was implemented, organized by architectural layer (e.g., SDK types, barrel exports, discovery, shared types, serialization, UI helpers). The summary should enumerate each file touched, what was added/changed, and how the layers connect. This applies especially after multi-file, cross-package changes. The user uses these summaries to orient themselves and confirm completeness before proceeding.

<!-- lore:019f2173-8b82-7e9a-92f6-11f392253c42 -->
* **Always think in terms of developer experience tooling and scaffolding CLI analogies**: When designing plugin or extension workflows, the user consistently frames solutions around developer experience (DX) tooling patterns: link/unlink scripts, \`package.json\` scripts, and CLI scaffolding tools analogous to \`create-next-app\` or \`create-hono-app\`. The user thinks ahead to how the workflow will be distributed (bundled with examples, then scaffolded via a CLI), and expects solutions to be portable across OSes. When proposing a dev workflow, always consider: (1) \`link\`/\`unlink\` npm scripts, (2) OS-agnostic path resolution, (3) how the tooling will be packaged and distributed to third-party developers, and (4) the long-term CLI scaffolding story.

<!-- lore:019f2183-8fff-7370-a294-1df96b6f07a4 -->
* **Always think through dev/prod collision and isolation problems before implementing**: When designing developer tooling or workflows, the user consistently raises and works through collision/isolation concerns before accepting an implementation. They identify when a dev artifact could conflict with a production artifact (same slug, same directory, same name), propose a naming/prefix strategy to separate them (e.g., \`dev-\` prefix), and expect the assistant to analyze all viable options with tradeoffs before recommending one. The user expects the assistant to proactively surface edge cases like 'what if prod version is already installed?' and evaluate solutions that avoid mutating source files or creating messy partial states.

<!-- lore:019f0eae-29cc-7c23-a6ec-68a03bb21d45 -->
* **Always think through plugin system architecture and developer workflows before implementing**: The user consistently approaches plugin system work by first asking exploratory/architectural questions (how to load plugins in dev mode, how to test local plugins, how to deploy plugins independently, how to structure plugin middleware) before committing to implementation. They think in terms of developer experience: local dev loading without npm publish, dev-mode discovery without enabling by default, separation of release pipelines per plugin. When proposing features, they come with a concrete mental model already formed. Respond by thoroughly exploring the existing architecture first (plan/read-only mode), then align proposals with their stated workflow preferences before making any changes.

<!-- lore:019f16d1-a59d-7fdf-aa4a-f06d0cc7d56a -->
* **Always trace complete end-to-end flows before asking for code changes**: When exploring a codebase, the user consistently wants to understand the full architectural flow from UI trigger through to the lowest-level implementation before any code is written or modified. They read multiple files across layers (UI → Electron main → HTTP routes → server services → core utilities), note key design decisions and invariants (e.g., atomic renames, no separate update path), and explicitly flag important comments from source code. Sessions are research-only until the full picture is clear. The assistant should proactively trace entire call chains, surface design decisions, and quote relevant code comments without waiting to be asked.

<!-- lore:019ee426-ee58-7080-b4ec-c648ff54861d -->
* **Always treat DESIGN.md as the authoritative source for UI rules and enforce its directives strictly**: When working on UI code in this project, the user expects strict adherence to DESIGN.md directives without deviation. Specifically: always use semantic CSS tokens (never raw hex values), never use pure #FFF or #000, always uppercase and letter-space JetBrains Mono micro-labels, and always use \`1px solid var(--border)\` for borders. These rules appear in every UI-related session and the user flags violations. Treat DESIGN.md as non-negotiable constraints, not suggestions — apply them proactively without waiting to be reminded.

<!-- lore:019f21eb-0a9c-7630-ad0b-b1e65c828e05 -->
* **Always update .lore.md when making significant changes**: The user consistently includes \`.lore.md\` updates in commits alongside functional code changes. Every time a meaningful feature, CI pipeline change, or architectural addition is made, \`.lore.md\` is modified in the same commit. This file appears to serve as a living project knowledge base. When implementing changes, always update \`.lore.md\` to reflect the new state — new packages, pipeline additions, architectural decisions, or configuration details — and include it in the same commit as the functional changes rather than as a separate documentation commit.

<!-- lore:019f14f9-cb57-7934-9b3e-a39d7f61249e -->
* **Always verify builds pass after applying code fixes before committing or closing out work**: After implementing any fix, the user consistently runs a full build (typecheck + compile) and test suite before considering the work done. This applies to both small targeted fixes (e.g., schema.ts transaction wrapping) and larger multi-file changes. The user expects: (1) typecheck to pass for all relevant tsconfig targets, (2) the electron-vite or turbo build to succeed with no errors, and (3) relevant tests (e.g., server tests) to pass. Only after all checks are green does the user commit, push, and update the PR. Always run and report build/test results before declaring a fix complete.

<!-- lore:019eea8d-9881-73af-8466-64366dcd401e -->
* **Always verify CI checks and PR state before squash-merging**: Before merging a PR, the user expects explicit verification of: (1) all CI checks passing with specific check names and durations noted, (2) PR merge state showing \`isDraft: false\`, \`mergeStateStatus: CLEAN\`, \`mergeable: MERGEABLE\`, \`state: OPEN\`, and (3) local HEAD matching the PR head commit SHA. Only after all three are confirmed does the user proceed with squash-merge into \`main\`. This applies consistently at the end of feature/fix branches before closing them out.

<!-- lore:019ee3b6-849a-7448-a2d6-14fa4ee42619 -->
* **Always verify completeness by checking for missed files or edge cases after mapping**: After receiving a comprehensive summary or mapping (e.g., all routes, all endpoints, full data flows), the user consistently follows up to verify nothing was missed — asking whether all files were covered, whether edge cases were handled, or whether the investigation is truly complete. When providing summaries of code structure, file lists, or system behavior, proactively note what was checked, confirm completeness explicitly, and flag any files or paths that could not be verified or may have been overlooked.

<!-- lore:019f14eb-9a28-7087-ae4b-d554815b7d25 -->
* **Always verify fixes with a full build and typecheck before considering work complete**: After implementing any code fix, the user consistently runs a full build (e.g., \`turbo build\` or the electron-specific build) and typechecks (e.g., \`tsc --noEmit\` for both \`tsconfig.node.json\` and \`tsconfig.web.json\`) to confirm the fix compiles cleanly. The assistant should proactively run or report these verification steps after applying changes, including noting bundle sizes, typecheck exit codes, and any pre-existing unrelated failures to distinguish them from regressions introduced by the fix.

<!-- lore:019f1653-ba60-75cc-ac0f-faf6bb76fe14 -->
* **Always verify implementation approach against official documentation before finalizing**: When the assistant proposes an implementation approach based on local type definitions, inferred behavior, or assumptions, the user explicitly asks whether the approach has been verified against official documentation. If not verified, the user expects the assistant to consult the official docs before proceeding. The assistant should then revise the plan based on what the docs actually recommend, replacing any inferred approach with the documented one. This applies especially to third-party library APIs (e.g., PostHog, Electron) where the correct usage pattern may differ from what type definitions alone suggest.

<!-- lore:019efafe-aacb-74ef-8eb3-a4407ef34e89 -->
* **Always verify pipeline correctness thoroughly before committing or pushing changes**: The user consistently wants all pipeline changes (GitHub Actions workflows, Craft configs, package.json edits) reviewed and validated in depth before any commit or push occurs. This includes tracing through source code of tools like Craft to confirm behavior (e.g., artifact matching logic), checking for collision risks, redundancy, and ordering issues. The user expects the assistant to proactively investigate edge cases and confirm correctness end-to-end. Only after a thorough review — with no unresolved concerns — should changes be committed and pushed. When in doubt, flag issues and wait for user confirmation rather than proceeding.

<!-- lore:019ee54a-f691-76c8-b358-5d8c5468e86a -->
* **Always work on feature/fix branches instead of committing directly to main**: The user consistently creates dedicated branches (e.g., 'fix/minor-imp') for their work rather than committing directly to main. When pre-existing uncommitted changes exist on main, they are carried over to the new branch. This pattern applies to both bug fixes and feature additions. When assisting with code changes, assume work is happening on a feature branch, not main. Commit messages and branch names should follow conventional patterns (e.g., 'fix/', 'feat/'). The user also tends to batch coherent, related changes together into a single branch/PR rather than mixing unrelated concerns.

<!-- lore:019f1651-6fea-7aa0-a987-ca92595c4185 -->
* **Always wrap analytics/error-reporting calls in try/catch and make them fire-and-forget**: The user consistently requires that all analytics and error-reporting calls (PostHog \`capture\`, \`captureException\`, etc.) are wrapped in try/catch blocks and never allowed to throw or block execution. This applies everywhere: route handlers, crash handlers (\`uncaughtException\`/\`unhandledRejection\`), renderer analytics, cloud sync cleanup, and pipeline error paths. The pattern includes: (1) try/catch around every PostHog call, (2) fire-and-forget for UI-side analytics (fetch with \`.catch(() => {})\`), (3) crash handler reporting wrapped so it cannot itself crash the crash path, and (4) comments like 'Never let analytics errors affect the app' and 'failures never interrupt the UI' to document intent. When writing or reviewing any analytics/reporting code, always apply this pattern unconditionally.

<!-- lore:019eea7b-4896-7134-9486-04eedb585cfe -->
* **Always write tests after implementing new functionality**: After implementing new features or fixes, the user expects tests to be written and run. In the current session, after creating \`disk.ts\` and integrating disk-space checks, the assistant verified existing tests still passed (149 tests) and noted existing test files — implying new tests for \`disk.ts\` should follow. Prior sessions confirm this pattern: tests are always run after changes, results are recorded, and new test files are added for new modules (e.g., \`mlx-runtime.test.ts\`, \`mlx-models.test.ts\`). When adding a new utility module, always create a corresponding test file in \`apps/server/tests/\` and run the full test suite to confirm 0 failures before committing.

<!-- lore:019f21b8-36e7-7608-8046-788ad56973a4 -->
* **Deslop plugin UI headers — remove redundant eyebrow/lede when info page exists**: When a plugin has a dedicated info/detail page in the main app (via \`freestyle.contributes.pages\`), the plugin's own UI page should NOT repeat the plugin description, eyebrow label, or lede paragraph. Remove those elements and keep only the functional content (title + primary action). The info page is the canonical place for plugin description. Applied to both \`profanity-filter\` (App.tsx) and \`audio-transcription\` (index.html + styles.css — removed \`.eyebrow\`, \`.title\`, \`.lede\` CSS classes entirely).

<!-- lore:019f214f-5fe2-7eac-89ec-054efd344c75 -->
* **Follow the established git workflow (branch, PR, review)**: Behavioral pattern detected across 3 sessions (action: enforced-workflow). The user consistently demonstrates this behavior.

<!-- lore:019f1672-dce2-7a00-a879-99011b32e81e -->
* **Mic stream should be released after each dictation session (not kept warm)**: User explicitly confirmed: mic stream should be released immediately after each dictation (current behavior via \`releaseStream()\` in \`cancelRecording\`). Do not change this to keep the stream warm between presses, even though it causes a \`getUserMedia\` round-trip on every hotkey press. Privacy (OS mic indicator off between sessions) takes priority over reducing init latency.

<!-- lore:019ef818-39a8-7346-bc15-eb0a35dd54c3 -->
* **Never commit real API keys or secrets**: Never commit real keys or secrets to the repository. \`.dev.vars\` is gitignored. For deployed Cloudflare Workers, use \`wrangler secret put \<KEY\_NAME>\`. This is an absolute directive stated across 6+ sessions.

<!-- lore:019f150b-3126-73aa-87aa-0baaf37bc803 -->
* **Never let analytics or background errors interrupt the main application flow**: The user consistently wraps all analytics, telemetry, and non-critical background operations in try/catch or .catch() to ensure they never throw or propagate. This applies to PostHog calls, cloud sync cleanup, renderer error forwarding, and any fire-and-forget side effects. The pattern is enforced with explicit code comments like 'Never let analytics errors affect the app' and 'never interrupt the pipeline/UI.' When implementing analytics calls, background syncs, or error reporting, always wrap them in try/catch that swallows or only logs errors, never re-throws, and never awaits in a way that could block or fail the primary response/render path.

<!-- lore:019f2428-af1c-7937-be28-d999a8d126e9 -->
* **Plugins never manage their own credentials — always route through host LLM endpoint**: User directive: plugins must never manage their own API keys or credentials. LLM access in plugins routes exclusively through the host's configured provider via \`PluginLlm\` on \`PluginContext\`. Always guard with \`if (ctx.llm)\` before use — the capability is optional and only present in server mode when the host has a default LLM configured. This was established during voice-commands plugin implementation.

<!-- lore:019f1711-492e-78f4-a483-694a96e1a398 -->
* **Prefer consolidating related changes into a single PR rather than splitting into smaller ones**: When the assistant flags that a refactor or change set is large and suggests splitting into smaller PRs for easier review, the user consistently chooses to include all related changes in the current PR instead. This applies to migrations, refactors, and feature work that spans multiple files. The user prioritizes completeness and consistency of the change set over PR size. Do not proactively suggest splitting work into smaller PRs — assume the user wants all related changes bundled together unless they explicitly ask to split. Confirmed twice: plugin update system + React Query migration (PR #355).

<!-- lore:019ee7e1-93b5-7ca8-a3b2-ff4dd8722903 -->
* **Prefer explicit, safety-first UI and diagnostics behavior**: User consistently wants UI and app behavior to fail safely. Exact directives from codebase comments (treated as design law): "Fire-and-forget: failures never interrupt the UI." (analytics.ts:13) and "Never let analytics errors affect the app" (posthog.ts:85,100). Analytics/telemetry must be best-effort with all errors swallowed in try/catch. Crash/diagnostic handlers must not recurse or break startup. Renderer error capture must route through IPC so main logs it without affecting renderer. PostHog kept as aggregate signal only — not a substitute for local file logs. Prefer practical diagnostics (file-backed logs, reveal/export actions, sanitized error capture) over aggregate-only telemetry.

<!-- lore:019efa2b-c3c4-7dfc-af14-8344d0d5e929 -->
* **Prefer making architectural decisions upfront before implementation begins**: When setting up a new system or pipeline, the user makes all key architectural decisions (versioning strategy, auth method, scope, trigger mechanism) explicitly before any files are created or changed. The user expects the assistant to investigate constraints, resolve blockers, and present a complete plan first — then confirm choices — before writing any configuration or code. Do not start creating files until the user has explicitly selected options for all major design dimensions. Present findings and options clearly, wait for selections, then proceed with implementation. Confirmed pattern: in Craft pipeline setup session, user answered 4 explicit design questions (versioning, auth, scope, trigger) before any files were touched.

<!-- lore:019f227a-fa7f-7007-a7a4-bd4018eaacee -->
* **Prefer real pointer/reference files over symlinks for cross-tool documentation**: When creating files that point one tool to another tool's documentation (e.g., CLAUDE.md pointing to AGENTS.md), the user prefers a real file containing a reference/pointer to the source file rather than a symlink that duplicates content. The file should explicitly direct the reader to the canonical source. This applies when setting up agent context files, documentation pointers, or any cross-tool discovery mechanism. Avoid symlinks; write an actual file with a clear reference statement.

<!-- lore:019f2195-6bc4-711e-9947-eb3eb08d2a30 -->
* **Prefer suffix over prefix for modifier tokens in naming schemes**: When naming dev/variant versions of slugs, package names, or identifiers, the user prefers appending a modifier as a suffix (e.g., \`slug-dev\`, \`package-dev\`) rather than prepending it as a prefix (e.g., \`dev-slug\`, \`dev-package\`). Rationale given: cleaner display names, easier slug handling. When proposing naming conventions for variants (dev, staging, local, etc.), always default to the suffix pattern. If a prefix-based scheme is already in place, expect the user to request migration to suffix form.

<!-- lore:019ee7d7-fb80-7811-8e04-d03f2cff08bc -->
* **Prefer targeted fixes and minimal changes over broad refactors**: When a UI or route issue is reported, user wants the smallest change that directly addresses the visible problem. They correct overengineering and ask to keep original structure. Specific pattern: when a spacing/alignment issue is reported, do NOT redesign the component — adjust the single CSS value (e.g. extend a line with \`-top-6\` rather than adding margins and rewriting spacing). User explicitly said "you're overthinking it — keep the original code and just extend the line." Make the narrowest safe edit, preserve existing behavior and layout unless explicitly asked for a larger refactor.

<!-- lore:019f224d-a85d-79fd-9462-44afd2765ab7 -->
* **Prefer turbo tasks over manual root scripts for monorepo commands**: Docs site (\`apps/docs\`) is excluded from default turbo tasks. \`pnpm dev\` and \`pnpm build\` use \`--filter=!docs\` to exclude docs. A separate \`dev:docs\` script runs \`turbo dev --filter=docs\` for the Mintlify dev server. User explicitly rejected docs running as part of the default \`dev\` command — docs should only run when explicitly calling \`pnpm run dev:docs\`. Do NOT add docs to the default dev pipeline.

<!-- lore:019f224d-a976-78a1-adcb-ad08bb5d4765 -->
* **Prefer turbo-based scripts over manual per-task scripts in root package.json**: When adding new scripts to the root package.json, the user prefers leveraging turbo's existing task pipeline (e.g., \`turbo dev\`, \`turbo build\`) to automatically pick up workspace packages, rather than adding manual per-package scripts (e.g., \`dev:docs\`). If a workspace package already has the relevant script (e.g., \`dev\`), turbo will run it automatically via workspace globs in pnpm-workspace.yaml. Avoid adding redundant manual scripts to root package.json when turbo can handle it. If a manual script is added, expect the user to redirect toward the turbo-based approach.

<!-- lore:019f1672-dce3-782d-8f9d-6a9ffe93ab8a -->
* **Restore system audio volume before playing stop tone (restore-then-tone ordering)**: User explicitly confirmed: when audio ducking is active, system volume must be restored before \`playTone('stop')\` fires. Ordering: \`await restoreSystemAudioSafely()\` → \`playTone('stop')\`. Rationale: tone plays at full volume (audible), not at \`DUCKED\_VOLUME=0.15\`. This is the mirror of the start-tone fix (play start tone before ducking begins).

<!-- lore:019f16fb-4f4b-76c2-9e0b-25aa750e8478 -->
* **Use @tanstack/react-query broadly in the renderer for data fetching with caching**: User added \`@tanstack/react-query\` to \`apps/electron\` (PR #355) for plugin update checks and explicitly noted it should be used broadly elsewhere too. \`QueryClientProvider\` is now present in \`dashboard.tsx\` wrapping \`\<TooltipProvider>\`. Default pattern: \`staleTime: ONE\_HOUR\` (3600000ms), \`retry: 1\` for network-dependent queries. Use \`useQuery\` for any renderer data that benefits from caching — not just plugin updates.

<!-- lore:019ef7a6-39c6-7a2d-b9d4-ed17c7101d67 -->
* **Use Craft-compatible commit message formatting**: Commit messages should always be written in a clean, proper format that Craft can parse and display correctly. Prefer concise conventional-style subjects (e.g. , , ) or the repository's established equivalent; avoid vague one-word subjects, noisy bot/AI labels, and malformed punctuation. When writing squash merge titles, PR titles, or normal commits, make the subject descriptive and Craft-friendly.

<!-- lore:019f21c7-5468-7ef0-a458-21a987ae2aa5 -->
* **Use react-query and react-hook-form in plugin UIs for maintainability**: User explicitly prefers using \`@tanstack/react-query\` and \`react-hook-form\` in plugin UIs even though they're not available from the host — plugins must bundle them. Install as \`devDependencies\` (Vite bundles them; not needed in npm package). Wrap app root with \`QueryClientProvider\` in \`main.tsx\`. Use \`useQuery\` for data fetching, \`useMutation\` + \`queryClient.invalidateQueries\` for mutations, \`useForm\` + \`register\` for forms. Applied to \`profanity-filter\` plugin in PR \`feat/plugin-ui-improvements\`.
